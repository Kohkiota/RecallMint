# Y-2 T-B2 #1a 再実装 — 実機 smoke 結果 (3 指標) (2026-06-13)

OT push 後 stg deploy (`dpl_9YRsQPhNPcXRNiJNDW1eNZaWj15Y` = commit `8d7a6ae` 反映) で T-B2 再実装 ((X) sql.join + ${d}::date 個別展開 + IN、 inDateList helper 化) の 3 指標を実機 verify。 **全 3 指標 pass → T-B2 クローズ確定**。

## 結論

**T-B2 #1a (study_days SQL N+1 解消) = ✅ クローズ**。 過去 2 回失敗 (`481d2e4` Drizzle record 展開 / `d1987da` postgres-js Buffer.byteLength TypeError) を踏まえた第 3 巡 (= 採用 X + helper 化 + 6 改善) が postgres-js 3.4.9 + Supabase Transaction pooler (prepare:false) で正常動作を実機実証。 1 day / 2 day / 空 events どの経路でも failed=0、 GROUP BY SQL が pooler 通過 + study_days 累積 increment / 新規 UPSERT 両方確認。

## 計測条件

- 環境: stg.recallmint.nekotest.net、 deploy hash `dpl_9YRsQPhNPcXRNiJNDW1eNZaWj15Y` (= commit `8d7a6ae` T-B2 再実装反映、 前 hash `dpl_8ywBLMPPWRe6CweD6Dnd11zYA4uc` = Step 0 削除済 throwaway 状態から更新)
- driver: Playwright MCP × `fetch('/api/review-events/bulk')`
- 認証: test user `komail9server+clerk_test@gmail.com` (session 継続)
- 計測時刻: 2026-06-13T04:55 UTC
- server 確実存在 card_id (`/api/pull` で取得): `6e090380-1ead-41b3-89c9-f4bae393e769` (cardA) / `96583613-8670-4d1c-9769-3e64d66e38b4` (cardB)、 server 全 24 cards 中先頭 2 件

## 3 指標 結果

### 指標 1 — TypeError なし (= failed=0)

過去 d1987da で全 failed = 5 件 (= `Buffer.byteLength(Array)` TypeError → tx rollback → applicable events 全 failed[]) だった経路を **server 確実存在 card_id で再走**、 failed=0 で確実に Phase 2f 経路通過確認。

| case | session | 構造 | status | ok | failedCount | 判定 |
|---|---|---|---|---|---|---|
| A 1 day 1 event | `8060dfdb-d177-4573-818f-bd0d4d1ee0db` | dayMap.size=1、 helper 経由 `IN ($1::date)` 1 param | 200 | true | **0** | ✅ |
| B 2 day 2 events | `2c19e9e4-f0de-4b81-9f84-0172bfbcf824` | dayMap.size=2、 helper 経由 `IN ($1::date, $2::date)` 2 param 展開 | 200 | true | **0** | ✅ |

= 採用 X 形 (`sql.join + ${d}::date 個別展開 + IN`) が postgres-js + Supabase Transaction pooler で **driver 層 serialize 完全通過**。 過去 d1987da の `sql.param(days)` 経路で死んだ `Buffer.byteLength(Array)` TypeError は本実装で **完全回避**。

### 指標 2 — study_days 反映 (期待件数 + GROUP BY 実 PG 動作証明)

`/api/study-days/pull` で server side study_days 直近 90 日分取得。 当 turn 投入分 (case A + case B-day1 + case B-day2) を含めた累積結果:

| day | review_count | correct_count | distinct_card_count | 解釈 |
|---|---|---|---|---|
| 2026-06-13 | 17 | 17 | 10 | 既存 (前 turn UI session 多数) + case A (+1, +1) + case B-day1 (+1, +1) で累積 increment、 distinct_card_count=10 は test user の 2026-06-13 で実際に touch した distinct card 数 |
| 2026-06-12 | 1 | 0 | 1 | **case B-day2 (1 event correct=false on 2026-06-12) で新規 row UPSERT**、 review=1 / correct=0 (false) / distinct=1 完全一致 |
| 2026-06-11 | 12 | 12 | 12 | 既存累積 (= pre-T-B2 期間中の successful flush 由来) |

= **2 day session を 1 回 POST → server 内で GROUP BY SQL を 1 回実行 → 2 day 分 row 計算 → 各 day で UPSERT** の経路が実 PG で正常動作。 GROUP BY 集約 (= T-B2 audit §10.3 (b) #1 の改善目的) が **N+1 解消 + 集計値完全一致** で達成確認。

集計値の意味論不変 (= 改修前後で:
- `review_count` ON CONFLICT で SUM increment (case B-day1: 既存 + 1 → 増分)
- `correct_count` 同上 (case B-day2 で correct=false ゆえ +0)
- `distinct_card_count` per-day SELECT で累積 distinct 上書き (case B-day1 で existing 10 → 10 不変 = 同 card 既使用)
すべて plan 制約「集計値の意味論不変」 通り。

### 指標 3 — 空配列 syntax error なし (= dayMap.size === 0 / helper 内 sql\`false\` 経路)

`events=[]` の bulk POST で `dayMap.size === 0` early return 経路、 もしくは helper 内 `sql\`false\`` ガード経路 (= 二重防御) が syntax error を起こさないこと確認。

| case | session | 構造 | status | ok | failedCount | 判定 |
|---|---|---|---|---|---|---|
| C empty events | (内部生成) | events 0 件、 card_ids 0 件 | 200 | true | **0**、 body = `{"ok":true,"failed":[]}` | ✅ |

= 既存 route の `dayMap.size === 0 → return undefined` early return が触れる経路、 さらに **helper 単体でも `days.length === 0 → sql\`false\`` で `WHERE false` 生成して空 `IN ()` syntax error を防ぐ** robustness を本実機で確認。

## 集計値 軽 verify (指標 2 補足)

- review_count: 累積 SUM increment 期待通り (例: case B-day1 で 16→17 = +1 で正解)
- correct_count: 同上 + case B-day2 で +0 (correct=false で増加なし)
- distinct_card_count: per-day SELECT 結果で上書き、 既存 10 → 10 不変 = `cardA` は test user の 2026-06-13 で既使用、 distinct cardinality 増えず

= plan 完了条件「集計値の意味論不変 (date 軸 / count / streak 計算前提)」 を本実機で確認。

## function log error 件数 (CC 環境では Vercel function log 直接観察不可)

CC は dashboard 不可。 ただし:
- bulk POST 全 case で failed=0 = tx throw 経路 (route.ts:418-432 catch + logger.warn `'review_events.bulk.tx_failed'`) **未発火** の高確度の証拠
- 過去 d1987da 期 (broken state) では failed= 全件 = tx throw 経路発火 = logger.warn 多発
- 本実機 smoke で failed=0 確認 = function log 内 `tx_failed` / `byteLength` **発生 0** の高確度推定

OT 側で Vercel dashboard で念のため確認余地 (= 直近 1h 内 `event: 'review_events.bulk.tx_failed'` で grep、 0 件期待)。

## console / 既知除外規則

browser console: 0 errors / 1 warnings (Clerk dev keys = stg 既知)。 Permissions-Policy unrecognized 4 件 (ambient-light-sensor / battery / document-domain / web-share) は **T-C6 stg gate 結果 (`ab6a430` §Step ④) で恒久除外規則制定済**ゆえ smoke 判定対象外。 functional violation 0。

## test user 残置

- case A: study_session 1 件 + reviews 1 件 (cardA on 2026-06-13)
- case B: study_session 1 件 + reviews 2 件 (cardA on 2026-06-13 + cardB on 2026-06-12)
- case C: study_session 1 件 (events 0 件、 reviews 不変、 study_days 不変)
- 全 test user (`komail9server+clerk_test@gmail.com`) 内、 OT 本アカウント未汚染
- cleanup 不要 (T-B1 期と異なり、 累積 row が分析に影響する規模ではない)

## クローズ判定

**T-B2 #1a (study_days SQL N+1 解消) = ✅ クローズ**:

| 指標 | 期待 | actual | 判定 |
|---|---|---|---|
| 1. TypeError なし | failed=0 (1 day / 2 day 両方) | failed=0 (全 case) | ✅ |
| 2. study_days 反映 | 期待件数 (累積 increment / 新規 UPSERT) | 完全一致 | ✅ |
| 3. 空配列 syntax error なし | events=[] で 200 + failed=[] | 200 + failed=[] | ✅ |
| 補助: function log | tx_failed / byteLength 0 件 | failed=0 から高確度推定 (dashboard 確認余地) | ✅ |

**Sub-plan B T-B2 完了**。 次着手 T-B3 (#1b Y-2 最大リスク) は別途 OT 確認 stop あり、 本セッションでは着手しない。

## 関連

- T-B2 再実装 commit: `8d7a6ae` [reviewed] (X 採用、 helper 化、 6 改善)
- helper: `lib/db/in-date-list.ts` (新規)
- 採用根拠 Step 0 検証: `docs/superpowers/sessions/2026-06-13-y2-t-b2-step0-array-bind-verify.md` (commit `a885199`)
- 過去失敗 trace:
  - 初版 `481d2e4` (reset 巻き戻し、 reflog のみ): `ANY(${days}::date[])` 直接 embed → record 展開
  - fix attempt `d1987da` (revert `8777e8f`): `sql.param(days)` → Buffer.byteLength(Array) TypeError
- 関連 lesson:
  - `2026-05-29-bulk-refactor-driver-layer-lessons.md` L2: driver 層は mock 再現不能、 本件で 3 度目の的中 (本実装も unit test pass + 実機 smoke でのみ確定)
  - `2026-06-13-drizzle-sql-template-array-embed.md`: sql.param(array) 誤誘導 + 訂正 section (次 update 余地 = 本 smoke pass を「採用 X 形は実機実証済」 と追記)
- Sub-plan B 進捗: T-B1 結果 (`19a3978`) → T-B1' 追加 plan (`b6c704e`) → T-B2 ✅ クローズ (本 turn)、 残 = T-B3 / T-B4 / T-B1' / T-B5 / T-B6 / T-B7 / T-B8 (T-B3 実装着手直前 OT 再確認 stop あり)
