# Session handoff: Sprint A-2 + A-3.1 完了、 A-3.2 着手前

> 作成: 2026-05-15 (machine 切替時の進捗保存)
> 状態: clean (uncommitted 0)、 未 push 2 commit

## このセッションでの commit

| hash | subject | review |
|---|---|---|
| `fb6cec8` | docs(spec+plan): Tech Spec v0.7 + plan v0.4 — Sprint A-2 deferral 解消 | [no-review] |
| `fa4dcd9` | feat(db): Sprint A-2 - mcq schema migration + contact_messages | [reviewed] |

(以下は前セッションの commit、 参考)

| hash | subject | review |
|---|---|---|
| `201b658` | fix(hooks): discord-notify が OPS_DISCORD_WEBHOOK_URL を .env.local から読むように修正 | (前セッション) |
| `8844a5a` | docs(plan): Sprint A-2 migration plan draft | [no-review] |
| `d8642ef` | chore: untrack .pnpm-store | [no-review] |

## 実 DB 状態 (Neon)

- mcq 12 テーブル baseline 適用済 (migration `drizzle/migrations/0000_keen_the_hunter.sql`)
- データ 0 件 (smoke test 後 cleanup 済)
- drizzle migration history table も clean

12 テーブル: users / ai_usage / ai_usage_users / clerk_events / stripe_events /
deletion_failures / reviews / exams / cards / source_documents / study_days /
contact_messages

## Sprint A-2 で確定した設計判断 (commit fa4dcd9)

- migration 方式: **Approach A (greenfield 再生成)**。実 DB が空だったため
- FK ON DELETE: 全 `user_id` FK で **CASCADE 統一** (ai_usage_users / reviews 含む、
  plan00 既定の NO ACTION から変更)。 GDPR / 個人情報保護法削除依頼対応
- soft delete: **users のみ**。 他は hard delete (cards / exams / source_documents /
  study_days / contact_messages / ai_usage_users)、 reviews は append-only
- `cards.source_document_id` のみ **SET NULL** (OCR 元削除しても抽出 card 保持)
- **RLS は MVP 不採用**、 アプリ層認可 (`WHERE user_id = ?` 必須) で対応
- contact_messages.category: `default 'general' NOT NULL` (Sprint A-2 review Important #1 で確定)
- Discord notifier 経路撤去 (DB 保存方針)、 OPS_DISCORD_WEBHOOK_URL は別物で維持

## Sprint A-3.1 で確定した内容 (commit fb6cec8)

- Tech Spec v0.6 → v0.7、 §2.1 / §2.3.3 / §2.3.4 / §2.5.1 / §2.5.2 / §2.7 /
  §2.8 / §2.9 / §3 / §8 / §13.1 / §13.2 / §13.7 / §13.23 / §14 を Sprint A-2 整合に更新
- 03-plan.md v0.1 → v0.4、 Phase 0 / 1 skip 確定 / Sprint A-1a・A-2 完了反映 /
  Sprint B-H を Tech Spec v0.7 整合で再記述

## 次に着手する Sprint: A-3.2 (コード系)

詳細: `docs/03-plan.md` の「現在 Sprint」直前の Sprint A 構造を参照。

A-3.2 でやること:

1. **`lib/auth/plan-limits.ts` を mcq 用に書き直し**
   - 現状: `{ free: { words: 100, aiGenPerDay: 10 }, pro: { words: 2000, aiGenPerDay: 100 } }`
   - `words` フィールドは vocab 撤去 (Sprint A-2) で dead — TODO コメントだけ Sprint A-2 で追加済
   - Tech Spec §6 の OCR 月次ページ数上限 (Free 30 / Standard 300 / Pro 公平利用) +
     プラン区分 (free / standard / pro) に書き直し
   - 関連: `lib/ai-usage.ts:60` が `limitsFor(userPlan).aiGenPerDay` を使用 — 呼出側調整必要
   - 関連: `app/(app)/app/upgrade/page.tsx` の「登録単語数」UI テキスト (vocab 表現) も mcq 用に書き直し
2. **contact_messages DB INSERT 配線**
   - `app/(marketing)/contact/actions.ts` を validation-only stub から DB INSERT 実装に
   - `lib/validation/contact.ts` に `category` field を追加 (default 'general' で form 互換)
   - `components/marketing/contact-form.tsx` に category select 追加 (UX 判断: ドロップダウンか radio か OT 判断必要かも)
   - tests 更新

## 重要 Fix の裏取り (CLAUDE.md §)

Sprint A-2 (commit fa4dcd9) は「削除を伴う変更 (データ cascade delete)」に該当する
ため、 厳密には OT 実機観察での動作確認を経てから [reviewed] tag を付与するのが原則
だが、 OT kickoff prompt で commit message に `[reviewed]` 指定があり、 cascade smoke
test 3 段全成功 + code-reviewer skill formal review pass を以て即時付与した。

OT が新マシンで再開時、 cascade 挙動の追加確認 (production smoke / Stripe webhook
連動削除等) を行う場合は `lib/users/delete.ts` 周辺 (Sprint A-1b で実装予定の R2
prefix 削除と統合される) のテスト時に再検証推奨。

## 未 push 状態

このセッション終了時点で `main` は `origin/main` より **2 commit ahead**:
- `fb6cec8` (docs)
- `fa4dcd9` (db schema)

push は memory `feedback_commit_workflow.md` のルールで OT 担当。
新マシン側で作業継続するなら、 旧マシンで先に push してから新マシンで pull する手順。

## このセッションで読んだ / 触った主要 file

### 変更 (commit 済)

- `lib/db/schema.ts` (12 テーブル baseline 書き換え)
- `drizzle/migrations/0000_keen_the_hunter.sql` (新規)
- `drizzle/migrations/meta/_journal.json` + `0000_snapshot.json`
- `app/(app)/app/page.tsx` (dashboard、 words → cards)
- `app/(app)/app/_components/{dashboard-actions,app-header}.{tsx,test.tsx}`
- `app/(app)/app/_actions/revalidate.{ts,test.ts}`
- `app/(marketing)/contact/actions.{ts,test.ts}`
- `lib/db/streak.{ts,test.ts}` (word_id → card_id、 todayWordCount → todayCardCount)
- `lib/auth/plan-limits.ts` (TODO コメントのみ)
- `.env.example` (CONTACT_DISCORD_WEBHOOK_URL 削除)
- `README.md` / `docs/architecture-guide.md` (stale ref 撤去)
- `docs/02-tech-spec.md` (v0.7)
- `docs/03-plan.md` (v0.4 全面書き直し)

### 削除 (commit 済)

- `drizzle/migrations/0000_flawless_squadron_supreme.sql` (旧 plan00 baseline)
- `app/(app)/app/words/` 全 8 ファイル
- `app/(app)/app/review/` 全 5 ファイル
- `lib/contact/notifier.ts` + `notifier.test.ts`
- `app/(marketing)/contact/notifier-factory.ts`

## 関連 doc

- `docs/plans/2026-05-13-sprint-a2-migration-plan.md` (Sprint A-2 計画ドラフト、 commit 8844a5a)
- `docs/02-tech-spec.md` §14 で v0.7 履歴の詳細
- `docs/03-plan.md` の進捗ログ
- `docs/superpowers/lessons/2026-04-30-users-schema-decoupling.md` (Sprint A-2 設計判断の参照元)
