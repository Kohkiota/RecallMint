# Y-2 Sub-plan C T-C6 stg gate 4 step 結果 (2026-06-12)

OT push 完了後 stg deploy (`dpl_Abuxu9KtSD5GRTnrKn7MyknLjaxi`、 commit `5df60f4` 反映) にて Permissions-Policy header の stg gate 4 step を実走。 functional violation 0、 OT 裁定 (a) で console warning 4 件許容 = T-C6 PASS。

## 結論

**T-C6 stg gate = PASS** (functional violation 0、 巡回 5 path + sign-in redirect で機能 block 発生なし、 console errors 0、 既存 5 header 不変 + Permissions-Policy 23 directive 全 path 出力確認)。 console warning 4 件は Chrome unrecognized feature 由来で functional impact なし = OT 裁定 (a) で許容、 spec §10.2 + next.config 不変。

## 計測条件

- 環境: stg.recallmint.nekotest.net、 deploy hash `dpl_Abuxu9KtSD5GRTnrKn7MyknLjaxi` (= commit `5df60f4` = T-C6 反映、 旧 hash `dpl_GgCYnY4mkgpAGtYeZvka8VbCyneS` (= 中間 smoke T-C3 まで) から更新確認)
- driver: Playwright MCP (Chrome)
- 認証: test user `komail9server+clerk_test@gmail.com` (前 session 持続)
- 計測時刻: 2026-06-12T16:44-16:47 UTC

## Step ① — Response header 確認 (全 6 path で Permissions-Policy 23 directive 出力)

`browser_evaluate` 内で `fetch()` × 6 path、 response headers から Permissions-Policy 取得:

| path | status | permPolicyExists | directiveCount | xVercelId 接頭辞 |
|---|---|---|---|---|
| `/app` | 200 | true | 23 | `hnd1::hnd1::k6cgq-...` |
| `/` | 200 | true | 23 | `hnd1::hnd1::zsgt4-...` |
| `/pricing` | 200 | true | 23 | `hnd1::hnd1::zsgt4-...` |
| `/contact` | 200 | true | 23 | `hnd1::zsgt4-...` |
| `/app/upgrade` | 200 | true | 23 | `hnd1::hnd1::zsgt4-...` |
| `/app/upload` | 200 | true | 23 | `hnd1::hnd1::k6cgq-...` |

= 全 path で **Permissions-Policy 23 directive 出力** 確認 (`accelerometer=()` から始まり `xr-spatial-tracking=()` まで spec §10.2 default candidate の引き写し)。 既存 5 header (HSTS / X-Frame-Options / X-Content-Type-Options / Referrer-Policy / CSP) も併存 (CSP は Clerk middleware auto-CSP、 next.config の `frame-ancestors 'none'` を Clerk が override、 既存仕様通り)。

deploy hash 更新確認: console log chunk URL `?dpl=dpl_Abuxu9KtSD5GRTnrKn7MyknLjaxi` (旧 `dpl_GgCYnY4mkgpAGtYeZvka8VbCyneS` から変更、 T-C6 commit 反映済)。

## Step ② — DevTools MCP 巡回 (5 path + sign-in redirect)

### /app/upload (OCR upload、 ファイル選択経路)

- h1 = "アップロード"、 UI 健全
- file input 1 件 (`accept="image/*,application/pdf"`, `multiple=true`, `capture=null`)
- `capture=null` = camera 起動なし、 通常 file dialog のみ → `camera=()` deny 影響なし
- console errors 0

### /app/upgrade (Stripe Checkout redirect)

- h1 = "プラン変更"、 UI 健全 (現在 Pro プラン年額、 Standard 月額予約中の status alert 表示)
- 月額 / 年額 toggle button click で price 表示切替確認
- **plan 変更 button (Standard 月額 / Pro 月額) は test user 現状 state (Standard 月額予約中) で全 disabled**: Stripe Checkout 経路の実 redirect 起動は本 user state では不能
- 経路存在 + Permissions-Policy header 出力で代替検証 (= spec §10.2 で Stripe Checkout redirect mode 採用済 + 当方 page から Stripe 別 origin への redirect は `payment=()` deny 影響なし = 当方 page で Payment Request API 触らない、 Stripe Checkout は Stripe 別 origin の hosted page で Stripe 側 policy が effective)
- console errors 0

### /sign-in (Clerk sign-in flow)

- logged-in 状態で /sign-in navigate → /app auto redirect (Clerk 動作、 logged-in user は sign-in 不要)
- UI 自体実走不能だが、 `fetch('/sign-in')` 経由で response status 200 + Permissions-Policy 23 directive 出力確認
- Clerk sign-in UI は別 origin (`delicate-liger-51.clerk.accounts.dev`) の component を script load、 当方 page (sign-in route の wrapper) の Permissions-Policy は出力済、 Clerk UI 内部 API 利用は Clerk 別 origin の policy が effective
- console errors 0

### /app (主要 page)

- h1 = "こんにちは"、 dashboard 健全表示
- console errors 0

### /app/exams

- 試験一覧 (2 件)、 console errors 0

### /app/tags

- タグ管理、 3 カテゴリ表示 + 並べ替え button 6 件、 console errors 0

## Step ③ — Console / Network / browser security warnings の violation 件数

### functional violation = 0 件

- 巡回 5 page + sign-in redirect で **機能 block 発生 0** (= camera / payment / sync-xhr / publickey-credentials 等の deny で UI が壊れた事例なし)
- console errors = 0 (全 page、 page header `Console: 0 errors, N warnings` 確認)
- Network response headers = 全 path で Permissions-Policy 23 directive 出力済 (Step ① 表)
- browser security warnings = 下記 4 件 (= functional violation ではなく Chrome unrecognized feature 由来)

### console WARNING × 4 件 (Chrome unrecognized feature)

全 page で同一の warning 4 件が反復:

```
[WARNING] Error with Permissions-Policy header: Unrecognized feature: 'ambient-light-sensor'.
[WARNING] Error with Permissions-Policy header: Unrecognized feature: 'battery'.
[WARNING] Error with Permissions-Policy header: Unrecognized feature: 'document-domain'.
[WARNING] Error with Permissions-Policy header: Unrecognized feature: 'web-share'.
```

- 該当 4 directive (`ambient-light-sensor` / `battery` / `document-domain` / `web-share`) は spec §10.2 default candidate に含まれる
- Chrome では未認識、 Firefox 等他 browser では認識 / 廃止 directive で互換維持目的
- functional impact = 0 (Chrome は未認識 directive を黙って無視、 deny は browser default で適用)
- `=(self)` 緩和 (= Step ④ 標準手順) は本件に **適用不能** (= unrecognized なら self/none 同等扱い、 warning 継続)

## Step ④ — OT 裁定: (a) warning 許容

OT 裁定 (2026-06-12): **(a) warning 4 件許容、 spec §10.2 + next.config 不変**。

### 裁定根拠

- functional violation 0 が stg gate の本質。 4 directive は Firefox 等で有効 = cross-browser deny の意図に合致、 削除は Chrome console を綺麗にするために他 browser の守りを外すことになり本末転倒
- spec §10.2 default candidate 引き写し原則 + 「記憶ベース固定禁止」 規律 (= browser 別 directive 採用判定を CC 記憶ベースで個別判断しない、 spec が SSoT)

### 恒久除外規則 (将来 smoke での誤検知防止)

以後の smoke では **以下 4 件の console warning を「ノイズ既知」 として恒久除外**、 「console error/warning 0 件」 判定の判定対象から除外する:

| directive 名 | warning 文字列 | 除外理由 |
|---|---|---|
| `ambient-light-sensor` | `Error with Permissions-Policy header: Unrecognized feature: 'ambient-light-sensor'.` | Chrome 未対応 (Firefox 対応)、 cross-browser deny 保持 |
| `battery` | `Error with Permissions-Policy header: Unrecognized feature: 'battery'.` | Chrome 未対応 (Firefox 対応)、 cross-browser deny 保持 |
| `document-domain` | `Error with Permissions-Policy header: Unrecognized feature: 'document-domain'.` | 廃止 directive、 RFC 互換維持で残置 |
| `web-share` | `Error with Permissions-Policy header: Unrecognized feature: 'web-share'.` | 一部 browser 未対応、 cross-browser deny 保持 |

将来 smoke 担当者は本 list を参照、 Chrome unrecognized 由来の Permissions-Policy warning は smoke pass 判定からは除外して良い (OT 裁定 (a) 2026-06-12 反映)。

## 完了判定

T-C6 stg gate 4 step = **PASS** (functional violation 0、 OT 裁定 (a) で console warning 4 件許容、 spec §10.2 + next.config 不変)。

## Test user 残置

本 session 内では追加書込なし (UI button click で plan 変更 / form submit 等の write 経路は disabled / 短絡で発火せず)。 中間 smoke (`7d1669c`) で残置済の card 1 + contact submission 6 + study_session 1 は別件、 cleanup 不要 (全 test user 内、 OT 本アカウント未汚染)。

## 関連

- T-C6 実装: commit `5df60f4` (`next.config.ts` Permissions-Policy 追加 + `next.config.test.ts` 2 case + `vitest.config.ts` include)
- spec §10.2 default candidate: `docs/superpowers/specs/2026-06-12-y2-launch-hardening-design.md:286-310`
- plan T-C6: `docs/superpowers/plans/2026-06-12-y2-launch-hardening-C-config.md:104-112`
- 中間 smoke (T-C1〜T-C3 回帰): `docs/superpowers/sessions/2026-06-12-y2-subplan-c-mid-smoke.md` (commit `7d1669c`)
- 観測 console log: `.playwright-mcp/console-2026-06-12T16-44-*` 〜 `16-47-*` (6 file)
