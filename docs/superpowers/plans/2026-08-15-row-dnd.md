# 行 DnD — テーブルビュー行ドラッグ並べ替え 実装 plan(r3 — Codex cross-check + OT 承認時修正 2 点 反映済み・**確定**)

> r3 = OT 承認時修正 2 点(2026-08-15): ① Task 4 test の `DragOverlay` を children passthrough stub 化(capture wrapper 経由の `onDragStart` 直接発火では実 DndContext の `active` が更新されず、real DragOverlay では正しい実装でも preview test が成立しないため)。Task 3 の real DndContext handle test は維持 ② Task 3 `RowDragHandle` の `aria-describedby` を dnd-kit attributes と合成し、spread で `lockedReasonId` が消えない実装にする(test は「含むこと」を assert)。

> Codex raw = `docs/codex/2026-08-15-plan-row-dnd.md`(独立 13 / 抜け 18 / リスク表 9)。抜け 18 件の採否 = **採用 12 / 部分採用 5(抜け 5 = 契約 comment で明示・到達不能理由の明文化 / 抜け 7 = preview cleanup pin のみ / 抜け 10 = overlay test 3 点のみ / 抜け 11 = pending 反映 pin のみ(既存入口の disable は既存 test が pin 済)/ 抜け 14 = click 遮断で経路網羅の根拠明記・実挙動は smoke #8)/ 不採用 1(抜け 8 = disabled button の支援技術到達性 — 凍結 spec §4.2 が既存 pattern(`exam-card-row-menu.tsx:116-119`)踏襲を確定済。a11y 改善は follow-up 起票)**。独立論点は 1(drag 中の順序変化)= spec §5.1-4 が「drop 時の現行順」を既に確定・並走順序変化は Grid-3 §2.5 受容 class(Task 4 に契約 1 行明文化)/ 2・3・9・13 = 抜け 3・T1 契約・抜け 9・抜け 17/18 に合流採用 / 5・8・12 = 既存機構・凍結契約で解決済(不採用)/ 4・6・10・11 = smoke へ委譲(§9 が既にカバー、実施注記のみ追加)/ 7 = 同一 tick 別入口は単一 pointer で物理不能・Grid-3 出荷済み構造(部分採用)。リスク表の「stale 順序の no-op vs 失敗 toast」は spec §2.2 の null 4 条件が既に確定(stale 検出という概念を持たない)— 対立なし。反映は各 task に「(Codex 抜け N 採用)」帰属付きで織込済。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 凍結 spec(`docs/superpowers/specs/2026-08-15-row-dnd-design.md`)を、常時 green の 5 task で実装する。

**Architecture:** 下層から積む — ① 順序 domain の pure 変換関数(Task 1)② sensors の lib/dnd 移設(Task 2)③ DnD UI 部品一式 + select 列 handle(Task 3)④ table 配線 + onDragEnd(Task 4)→ 完了 gate + session doc(Task 5)。**DB / wire / server / sync / `useMoveCards` は全 task で不触** — 変更対象は client UI + pure domain のみ。Task 3 の部品は context 未提供時に自滅描画(null)するため、Task 4 の配線前でも常時 green。

**Tech Stack:** `@dnd-kit/core 6.3.1` / `@dnd-kit/sortable 10.0.0`(既存 pin のまま・`@dnd-kit/modifiers` 追加禁止)/ TanStack Virtual(設定不触)/ Vitest + fake-indexeddb(実 drag は jsdom 不可 → stg smoke 委譲)。

**Spec:** `docs/superpowers/specs/2026-08-15-row-dnd-design.md`(確定・凍結。§5.1 の onDragEnd 6 手順と §8 の pin 一覧が正本)

## Global Constraints(全 task に適用)

- spec は凍結。仕様判断が必要になったら停止して OT 相談。順序契約・sync 契約に触れる必要が出たら停止(kickoff 決定 11)。
- **DB migration なし・wire 変更なし・server 変更なし・`useMoveCards` 無変更**(spec §7)。
- gating 判定は既存 `positionLocked`(`exam-card-table.tsx:514`)のみ。新判定を作らない。
- DndContext は**常時 mount**(conditional mount 禁止 — remount tear-down、spec D-b)。無効化は handle gate + `useSortable` disabled + onDragEnd 再検査の 3 層。
- 文言は spec 仮置き 3 本を使用: 成功「並び順を変更しました」/ 失敗「並べ替えに失敗しました」/ `ROW_DND_LOCKED_REASON`(spec §4.2)。
- **feat task(Task 1 / 3 / 4)**= canonical review(superpowers:requesting-code-review 既定経路)+ Codex(`scripts/ai/codex-review.sh <topic>`)並列 → 未解決 Critical 0・Important 0 → `[reviewed]` commit。**Task 2 のみ例外**: ロジック変更なしの移設・改名 = CLAUDE.md refactor 例外で `[no-review]`(この例外指定が本行の feat 規定に優先 — Codex 抜け 16 採用)。`--no-verify` 全面禁止。implementer subagent は commit しない(working tree で返し、controller が review pass 後に commit)。
- 実 pointer drag は jsdom で再現しない(既存前例)。実 drag・auto-scroll・sticky・touch は stg smoke(spec §9 — **全 FAIL prod blocker**)へ委譲。

---

### Task 1: 順序 domain — `placementForRowDrop` pure 関数 + unit

- 目的: `lib/cards/domain/card-order.ts`(既存 pure module)に spec §2 の変換関数を追加する。export 1 点:
  - `placementForRowDrop(baseOrderIds: string[], activeId: string, overId: string | null): MovePlacement | null` — 最終列 = `arrayMove(baseOrderIds, indexOf(activeId), indexOf(overId))` 相当(**arrayMove は自前数行** — dnd-kit を import しない)。最終列で active が index 0 → `{kind:'start'}` / それ以外 → `{kind:'after', anchorId: 最終列[activeIndex−1]}`(末尾 drop も after に一様化 — spec D-f)。null 条件 4 つ(spec §2.2): overId null / activeId===overId / いずれかが baseOrderIds に不在 / 最終列が入力列と一致。
  - **呼出側契約を doc comment に明文化**(Codex 独立 3 採用): `baseOrderIds` は mirror PK 由来の**一意** id 列・対象 exam の**全件**・`compareByBaseOrder` の基準順。重複 id は契約外(呼出側 = Dexie PK read で構造的に排除。防御 dedup は書かない — 簡潔性規律)。条件④(最終列一致)は一意契約下では ①〜③ に包含される **defensive invariant** であり、その旨を comment に記す(Codex 抜け 5 部分採用)。
- 制約: 既存 PURE 制約(I/O なし・入力非破壊・dnd-kit 含む import 追加なし)。既存 export(`MovePlacement` / `planMoveAssignments` 等)は不触。
- test(`card-order.test.ts` 拡張 — spec §8-1): 下→上 / 上→下 / 先頭へ / 末尾へ / 隣接 swap / 2 件列 / over=null / active===over / activeId 不在 / overId 不在 → null / **`planMoveAssignments` 合成 round-trip**(返した placement + 対応する OrderedCard 列を食わせて throw しない・移動対象の挿入位置が最終列の active 位置と一致 — 上下両方向)。
- 完了条件: 対象 unit green + typecheck + canonical/Codex Crit0・Imp0 + [reviewed]。

### Task 2: sensors 移設・改名(lib/dnd 新設・挙動不変)

- 目的: `lib/tags/use-tag-sortable-sensors.ts` → **`lib/dnd/use-sortable-sensors.ts`**、`useTagSortableSensors` → **`useSortableSensors`**(spec §6)。test も `lib/dnd/use-sortable-sensors.test.ts` へ移動し import / describe 名を追随。既存 3 site(`category-list.tsx:40` / `option-list.tsx:44` / `card-tag-add-popover.tsx:45`)+ hook 参照コメント 2 箇所(`category-list.tsx:196` / `option-list.tsx:136`)の import / 文言を更新。header コメントの「3 site 共有」記述は「tag 3 site + 行 DnD の 4 site 共有」に更新。
- 制約: sensor 構成・options は**変更禁止**(Mouse 即時 / Touch `{delay:250, tolerance:5}` / Keyboard `sortableKeyboardCoordinates`)。`lib/tags/` に re-export shim を残さない(参照は全て新 path へ)。既存 test の 4 assert は内容不変で移設(= 保証不変の宣言対象)。
- 完了条件: whole `pnpm test` green + lint + typecheck。commit = `refactor(dnd): sensors 共有 hook を lib/dnd へ移設・改名(挙動不変)` + **[no-review]**(Global Constraints の例外指定どおり。message に test「保証不変」を明記)。

### Task 3: DnD UI 部品一式(`exam-card-row-dnd.tsx` 新設)+ select 列 handle 配線

- 目的: spec §3.2〜§3.6 / §4.2 の部品を新 file に置き、select 列へ handle を配線する(この時点で provider 未配線 = handle は null 描画・視覚変化なし = 独立 green)。
  - `lib/dnd/restrict-to-vertical-axis.ts`: `export const restrictToVerticalAxis: Modifier = ({ transform }) => ({ ...transform, x: 0 })`(core の `Modifier` 型)+ mini unit(x=0 固定・y/scaleX/scaleY 素通り)。
  - `app/(app)/app/exams/[id]/_components/exam-card-row-dnd.tsx`('use client' なし — 親からのみ import される子、row-menu と同 pattern):
    - `type RowDndValue = { listeners: SyntheticListenerMap | undefined; attributes: DraggableAttributes; setActivatorNodeRef: (el: HTMLElement | null) => void; showHandle: boolean; locked: boolean; pending: boolean; lockedReasonId: string }` / `RowDndContext = createContext<RowDndValue | null>(null)`(既定 null)。
    - `SortableRow({ cardId, index, showHandle, locked, pending, lockedReasonId, measureElement, children }: { cardId: string; index: number; showHandle: boolean; locked: boolean; pending: boolean; lockedReasonId: string; measureElement: (node: Element | null) => void; children: ReactNode })` — 内部で `disabled = locked || pending` を導出し `useSortable({ id: cardId, disabled })`(spec §3.2 の条件と同値・prop 重複を作らない)。`<tr data-index={index} data-testid={"row-"+cardId} className="group hover:bg-muted/50">` は現行 TableBody の verbatim 移送 + `isDragging && 'opacity-50'`。`setNodeRef` と `measureElement` は **useCallback で identity 固定した merge ref**(spec §3.2 — inline arrow 禁止: 毎 render の detach/attach で ResizeObserver が張り直される)。style = `CSS.Transform.toString(transform)` + transition。`attributes` / `listeners` は tr に乗せず provider で配る。
    - `RowDragHandle({ title }: { title: string })` — context null または `!showHandle` → null。`GripVertical` + `cursor-grab` + **`touch-none`(handle のみ — event 分離契約)** + `onClick={(e) => e.stopPropagation()}`(select td 全域の行選択トグル対策。td 側 handler は **onClick のみ**(`exam-card-table.tsx:218-220`)のため click 経路の遮断が mouse / keyboard / tap の全 activation 経路をカバーする — Codex 抜け 14 部分採用。drag 後 click・touch 実挙動は smoke #8)+ `aria-label={"行を並べ替え: "+title}` + `disabled = locked || pending` + locked 時のみ `title={ROW_DND_LOCKED_REASON}`。`ref={setActivatorNodeRef}` + `{...listeners} {...attributes}`。
      - **`aria-describedby` は合成する**(2026-08-15 OT 承認時修正): dnd-kit の `attributes` は自前の `aria-describedby`(SR instructions 要素 id)を**既に持つ**(`core.esm.js:3432-3439`)。素朴に `{...attributes}` の後へ `aria-describedby={lockedReasonId}` を書くと dnd-kit 側が消え、前に書くと spread に上書きされてこちらが消える。**`{...attributes}` の後に、両者を空白区切りで結合した合成値を明示的に置く**: `aria-describedby={[attributes['aria-describedby'], locked ? lockedReasonId : null].filter(Boolean).join(' ') || undefined}`(locked でなければ dnd-kit 側だけが残る)。この修正は「disabled button の支援技術到達性」follow-up とは別件(属性が消えること自体の欠陥)。
    - `RowDragPreview({ card }: { card: ClientCard })` — grip icon + **番号(question_label・あれば)と タイトル(title 先頭 40 字)の両方**を並記(両方空なら `(無題)`。`??` 片方表示は kickoff 決定 3「番号・タイトルを示す」とずれるため不可 — Codex 抜け 9 採用)。簡素 div(table row を再現しない)。
    - `export const ROW_DND_LOCKED_REASON = 'ソート/フィルタ適用中は並べ替えできません(解除すると並べ替えられます)'`。
  - `exam-card-table-columns.tsx`: select cell の先頭(checkbox の前)に `<RowDragHandle title={card.title} />`、select 列 `size: 88 → 112`。
- 制約: TableBody(`exam-card-table.tsx`)は不触(SortableRow の使用開始は Task 4)。handle 以外の要素(tr / checkbox / 開くボタン / 行メニュー)に `touch-none` / dnd-kit attributes を付けない。
- test(新設 `exam-card-row-dnd.test.tsx`): DndContext + SortableContext 最小 wrapper 下で `<table><tbody><SortableRow>` + RowDragHandle を render — ① `aria-roledescription="sortable"` / `touch-none` が handle button のみ(tr・他 button に無し — tag 先例 `category-list.test.tsx:590-` と同型)② handle click で外側 onClick spy 不発火(stopPropagation)③ context 未提供で handle 非描画 ④ `showHandle:false` で非描画 ⑤ `locked:true` で disabled + title + **`aria-describedby` が `lockedReasonId` を含む**(合成 pin: dnd-kit 由来 id を消していない = 値が `lockedReasonId` 単独でないことも併せて assert)、`pending:true` で disabled のみ(title なし・`aria-describedby` に `lockedReasonId` を含まない)⑥ **同一 props での再 render で `measureElement` spy が再呼出しされない**(merge ref の identity 安定 pin — OT 承認事項 ③ / Codex 抜け 12 採用)。columns test: select 列 size 112。
  - 本 test は **real DndContext / real SortableContext** を使う(handle の attributes / listeners は実物であることに意味がある)。
- 完了条件: 対象 unit green + lint / typecheck + canonical/Codex Crit0・Imp0 + [reviewed]。

### Task 4: table 配線 + onDragEnd(`exam-card-table.tsx`)+ 接続契約 test

- 目的: spec §3.1 / §3.5 / §4 / §5 の配線を行う。
  - DndContext(**常時 mount**): `sensors={useSortableSensors()}` / `collisionDetection={closestCenter}` / `modifiers={[restrictToVerticalAxis]}` / `onDragStart` / `onDragEnd` / `onDragCancel`。**`autoScroll` は渡さない**(既定 — kickoff 決定 10)。配置は既存 root div 直下(scroll container + table + toast を包含)。
  - SortableContext: `items = useMemo(() => data.map((r) => r.card.id), [data])`(基準順全件)/ `strategy={verticalListSortingStrategy}`。`<table>` の外側。
  - TableBody: 行 `<tr>` を `SortableRow` に差し替え(props: cardId / index=vi.index / showHandle=`data.length >= 2` / locked=`positionLocked` / pending=`movePending` / lockedReasonId / measureElement=`rowVirtualizer.measureElement`)。`TableBodyProps` に必要値を追加(MemoizedTableBody の comparator `next.isResizing` は不触)。**既知挙動として記録**(Codex 抜け 13 採用・コード変更なし): resize 凍結中は SortableRow への locked/pending/showHandle 反映が commit まで遅延するが、resize と drag/state 変化は単一 pointer で同時発生せず無害。実挙動は smoke #9 で確認。
  - DragOverlay: `createPortal(<DragOverlay>{activeDragCard ? <RowDragPreview card={activeDragCard}/> : null}</DragOverlay>, document.body)`(`typeof document` guard — PullIntoDialog と同型)。`activeDragCard: ClientCard | null` state — `onDragStart` で `data` から正規化済 id で引いて set(**不在なら null のまま = preview 非表示**)、`onDragEnd` / `onDragCancel` で null。dropAnimation 既定。
  - `lockedReasonId = useId()` + sr-only `<p id={lockedReasonId}>{ROW_DND_LOCKED_REASON}</p>` を table レベルに 1 個。
  - **id 正規化は onDragStart / onDragEnd の冒頭 1 箇所**(Codex 抜け 3 採用): `const activeId = String(active.id)` / `const overId = over ? String(over.id) : null` を作り、以降の placement・moveCards・activeDragCard 検索の全てで同じ正規化値を使う(dnd-kit の `UniqueIdentifier` は number を許すため境界で string に固定)。
  - `handleRowDragEnd`(spec §5.1 の 6 手順**そのまま** — 順序変更禁止): ① `dragCommitRef.current` 再入検査(読むだけ)② activeDragCard クリア ③ `positionLocked || movePending` → 破棄 ④ `placementForRowDrop(data.map((r) => r.card.id), activeId, overId)` → null なら無音 return ⑤ **ここで** `dragCommitRef.current = true` + `setMovePending(true)`(最初の await 直前)⑥ try `moveCards({ cardIds: [activeId], targetExamId: examId, placement })` → ok なら `showMoveToast('並び順を変更しました', result)` / outcome≠ok・reject は一律 `showMoveToast('並べ替えに失敗しました')`(undo なし — spec D-e)、finally で ref 解除 + `setMovePending(false)`。既存 `runMove` は**使わない**(spec D-d)。**契約 1 行を実装 comment に明記**(Codex 抜け 2 / 独立 1 採用): placement は **drop 時の現行順**(spec §5.1-4 の `data`)に対して解く。drag 中の並走順序変化は「位置ずれのみ・順序は決定的」の Grid-3 §2.5 受容 class であり、stale 検出は行わない(null 4 条件が凍結契約)。
- 制約: 既存 3 入口(`runMove` / `dispatchMove` / `onPullInto`)・toast slot・`onUndoMove`・selection prune・virtualizer 設定(overscan 5 / estimateSize / getItemKey / useFlushSync)は不触。`useMoveCards` 無変更。
- test(新設 `exam-card-table-dnd.test.tsx` — `exam-card-table-move.test.tsx` の mock 構成(useMoveCards spy + fake-indexeddb mirror + ControlledExamCardTable)を踏襲。`@dnd-kit/core` は **partial mock 2 段**(`importOriginal` spread):
  - `DndContext` = **実 DndContext を内側に保持する capture wrapper**(`(props) => { captured = props; return createElement(実DndContext, props) }`)— children 素通し stub は `useSortable` が実 context を失い handle attributes が本物と乖離するため不可(Codex 抜け 4 採用)。
  - `DragOverlay` = **children passthrough stub**(2026-08-15 OT 承認時修正)。捕捉した `onDragStart` を直接発火しても**実 DndContext 内部の `active` state は更新されない**ため、real DragOverlay は children を描画せず、正しい実装でも preview test(⑩⑪)が成立しない。overlay の**中身の描画契約**を pin する test なので、DragOverlay 自身の表示機構は stub 化し、preview の実挙動は smoke へ委譲する。
  - 捕捉した `onDragStart` / `onDragEnd` / `onDragCancel` を合成 event `{ active: { id }, over: { id } | null }` で手動発火): spec §8-4 の全ケース —
  ① 有効 drop で `moveCards` が `{ cardIds: [activeId], targetExamId: EXAM_ID, placement }` で 1 回(**行選択 2 行を立てた状態で非選択行を drag しても cardIds は `[activeId]` のみ**)② **同一 tick 2 発 dispatch で moveCards 1 回**(await を挟まず捕捉 handler を 2 回同期呼出)③ over=null / active===over で未発行・toast なし ④ **同位置 no-op の後の有効 drop が正常発行**(ref 残置なしの pin)⑤ 成功で「並び順を変更しました」+ [元に戻す] → `undoMove` 配線 ⑥ reject / `{ok:false}` で「並べ替えに失敗しました」+ undo ボタン非表示 ⑦ **reject 後の再試行が発行される**(finally 解除の pin)⑧ sorting 適用中(header menu 経由で sort 適用): handle `disabled` + `aria-describedby` + onDragEnd 手動発火が破棄(moveCards 不発火)⑨ data 1 件で handle 非描画 ⑩ **onDragStart で preview(番号 + タイトル)表示 → onDragEnd / onDragCancel で消える・cancel は moveCards 不発火 + toast なし**(Codex 抜け 6・7 採用)⑪ **stale active id(data に無い id)の onDragStart で preview 非表示**(Codex 抜け 10 部分採用)⑫ **moveCards 未解決中は handle disabled(pending 反映)**(Codex 抜け 11 部分採用。既存入口の pending disable は既存 test が pin 済)⑬ **data 1 件 → mirror へ 2 件目 insert で handle が出現する**(rows 動的変化 — Codex 抜け 15 採用)。
- 完了条件: 対象 unit green + lint / typecheck + canonical/Codex Crit0・Imp0 + [reviewed]。

### Task 5: 完了 gate + session doc

- 目的: sprint 完了 gate を whole-repo で実走し、session doc を書く。
  - gate: `pnpm lint`(--max-warnings=0)/ `pnpm typecheck` / `pnpm build`(postbuild の pdfium 検証込み)/ `pnpm test` / `pnpm test:iso`(無条件)/ `pnpm run audit` — 全 exit 0。依存・Next 設定 file 不触のため `--frozen-lockfile` 系は対象外。
  - session doc: `docs/superpowers/sessions/2026-08-15-row-dnd.md` — task 別 review 結果 / 裁定 / gate 結果 / spec §9 smoke 12 項の実施待ち一覧。
  - **Task 5 完了 = 実装完了であって prod-ready ではない**(Codex 抜け 1 採用): prod-ready 判定は push 後の stg smoke 12 項 **all-pass** が前提(spec §9 — 全 FAIL prod blocker)。smoke は標準フロー(push 後 OT 指示で CC が Playwright MCP 実走)。
  - **smoke 実施要領を session doc に前置**(Codex 独立 13 / 抜け 17・18 採用): ① 各項目で操作前後の DB readback(基準順・base_order)+ 使用 card/exam id + Network reqid を記録 ② 同位置 no-op(#2)の判定は outbox 総件数比較でなく**直前 snapshot(max local_id / mutation_id)対比で新規行ゼロ + 無 request**で行う ③ 1200 枚(#7)は端末・viewport 条件を記録 ④ **FAIL 時の分岐**: FAIL を記録 → 追加修正に着手しない → 証跡(数値・スクリーンショット・reqid)保存 → OT へ報告 → prod 移行停止。凍結契約の変更が要る FAIL は停止して OT 相談(spec §9)。
- 完了条件: 全 gate exit 0 の記録 + docs commit(`docs(sessions)` + [no-review])+ stop checkpoint 報告(chat に「whole-repo lint exit 0 / test:iso green / pnpm run audit exit 0 確認済」を明記)で CC 停止 → OT push 判断。
