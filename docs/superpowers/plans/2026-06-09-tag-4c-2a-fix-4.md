# Tag-4c-2a-fix-4: タグ popover の幅一貫性修正 (min-w-56 + break-all + 型選択 二重 px-2 解消) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`。 各 task 完了条件は `pnpm test` + `pnpm build` 緑。 全 task 完了後に Tag-4c-2a 系 feat commit `0be3064` (= HEAD~3) に **`git commit --amend --no-edit` で畳む** (別 commit を積まない、 Task 3 で実行)。 push は OT が `--force-with-lease` で実施 (Claude Code は push しない)。

**Goal:** タグ popover (`CardTagAddPopover` / `CardTagEditPopover`) の (1) PopoverContent を `w-auto` → `min-w-56 max-w-sm` に変更して幅 floor を確保、 (2) row text (color pill + 新規作成行 span) に `break-all` を追加して長 Japanese 名で popover を max-w-sm 超えに膨張させない、 (3) 型選択 stage の 2-button outer の二重 px-2 (outer + button class) を outer 削除で解消し他 stage の row content と縦揃え一致させる。 全 fix が見た目 (CSS) のみ、 logic / state / handler / event flow 一切不変。

**Architecture:** Fix-1 = `<PopoverContent className="w-auto max-w-sm p-0">` → `<PopoverContent className="min-w-56 max-w-sm p-0">` (両 popover)、 224px floor + 384px cap で stage 遷移時の幅変動を抑制。 Fix-2 = `CardTagOptionList` の color pill `<span>` + 新規作成行 `<span>` に `break-all` を追加し連続 Japanese 漢字 (no word boundary) でも `max-w-sm` 内で wrap。 Fix-3 = 型選択 stage の 2-button outer `<div className="px-2 pb-1">` → `<div className="pb-1">` (`px-2` のみ削除、 `pb-1` 維持)、 button content 左端が popover 左から 16px → 8px に下がり他 stage の row content と整合。

**Tech Stack:** Next.js 15 / Tailwind v4 (既存) / shadcn Popover (既存) / lucide-react (既存) / Vitest + RTL。 **npm dep 追加なし**。

**前提:**
- Tag-4c-2a-fix-3 着地済 (commit `0be3064`、 4 fix = width 揃え試行 + 見出し削除 + multi icon CheckSquare + category 行 select_type icon)
- 既存 server apply 経路 / schema / `lib/client-db.ts` 無改修
- 既存 `tags/_components/*` (manager) 据置
- 既存資産流用: `CardTagOptionList` row JSX 構造 (color pill + name + 新規作成行)、 `CardTagAddPopover` / `CardTagEditPopover` の PopoverContent
- spec: `docs/superpowers/specs/2026-06-09-tag-4c-2a-fix-4-design.md`
- Step 0 結果: red flag 0 件、 全 fix feasible、 真因 (stage B が広い) は empty placeholder text の幅と確定 (back button ではない)

**維持する設計 (Tag-4b-fix / 4c-1 / 4c-2a / 4c-2a-fix / 4c-2a-fix-2 / 4c-2a-fix-3 継承、 不変):**
- whole-set 不変条件 / single 最大 1 個・0 個許容 / 案 a 取り直し / parent 一括 subscribe + useMemo + 子 `React.memo` / popover stage + Esc 階層 (5 stage、 createCategoryType 含む) / npm dep 追加ゼロ / user_id 親 prop / atomic 戦略 / 既存 manager 据置 / option 作成挙動不変
- multi default focus + Enter 即決定 / 二重発火ガード / category 同名抑制 / 編集 stage 全選択 focus rAF / category 行 select_type icon / 型選択 stage コンパクト 2 button
- **本 sprint は全 fix が見た目 (CSS) のみ**、 logic / state / handler / event flow / イベント順序 一切不変

**全 task 共通ルール:**

- TypeScript strict、 既存 Client Component pattern 踏襲
- 全 UI 文言は日本語、 Tailwind class は既存 slate 系で統一
- 各 task は **未 commit** のまま review、 Task 3 末で全変更を `0be3064` に amend で畳む
- amend 後の commit message は `--no-edit` (既存維持)
- push しない (Claude Code が `git push` を実行しない、 OT が `git push --force-with-lease origin develop` で実施)

---

### Task 1: `CardTagOptionList` の color pill + 新規作成行 span に `break-all` を追加 (Fix-2)

**Files:**
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-option-list.tsx`
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-option-list.test.tsx`

**目的:** 長 Japanese option / category 名 (例「肺気腫合併心房細動」、 連続漢字で word boundary なし) が color pill / 新規作成行で wrap せず popover を `max-w-sm` 超えで膨張させる問題を、 `break-all` で任意位置 break 可能にする。

**制約:**

- `card-tag-option-list.tsx:236-244` の color pill `<span>` class:
  - 現状: `'inline-flex items-center rounded border px-1.5 py-0.5 text-xs font-medium'`
  - 修正後: `'inline-flex items-center rounded border px-1.5 py-0.5 text-xs font-medium break-all'`
  - `break-all` (= CSS `word-break: break-all`): 任意位置 (Japanese 文字間含む) で break 可能、 word boundary に依存しない
- `card-tag-option-list.tsx:284-298` 周辺の新規作成行 `<span>新規作成: {trimmed}</span>` class:
  - 現状: 未指定 (default `white-space: normal` のみ)
  - 修正後: `<span className="break-all">新規作成: {trimmed}</span>`
  - 長 Japanese 入力時 (例 `trimmed` = 「肺気腫合併心房細動の急性増悪」) に max-w-sm 内 wrap
- empty placeholder `<p>` (line 185-188) は**修正不要**: default text には全角 spaces / 句読点で wrap 効く、 Fix-1 の min-w-56 + max-w-sm で間接解決
- 既存 row click / kebab / filter / 新規作成行 click / select_type icon (Tag-4c-2a-fix-3 Fix-4) の挙動は完全不変
- `break-words` (= `overflow-wrap: break-word`) は採用しない: 英文 word boundary 優先、 Japanese で効きが弱い (Spec §設計判断 2)
- 既存 import / state / handler / event flow 完全無改修

**完了条件:**

- color pill `<span>` class string に `break-all` が含まれる (grep / DOM 検査)
- 新規作成行 `<span>` class string に `break-all` が含まれる
- test:
  - color pill の class 文字列に `break-all` が含まれる (test で class assertion)
  - 新規作成行 span の class 文字列に `break-all` が含まれる
  - 長 option 名 (例「肺気腫合併心房細動」) を render しても DOM 構造に変化なし (regression、 break-all はあくまで wrap 制御で text 自体は不変)
  - 既存 row click → onToggle、 kebab → onRowAction、 新規作成行 click → onCreateNew、 select_type icon 表示、 filter 動作 全て regression なし
- `pnpm test` 全件緑、 `pnpm build` 緑、 tsc strict 緑

---

### Task 2: PopoverContent を `min-w-56 max-w-sm` に + 型選択 stage 二重 px-2 解消 (Fix-1 + Fix-3)

**Files:**
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-add-popover.tsx`
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-edit-popover.tsx`
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-add-popover.test.tsx`
- Modify: `app/(app)/app/exams/[id]/_components/card-tag-edit-popover.test.tsx`

**目的:** PopoverContent に min-w-56 (224px) floor を追加し stage 遷移時の幅収縮を防ぐ (Fix-1)。 型選択 stage の 2-button outer から余分 `px-2` を削除し button content 左端を他 stage の row content (8px) に揃える (Fix-3)。 全て見た目のみ、 logic / state / handler / event flow / イベント順序 完全不変。

**制約:**

- **Fix-1 (両 popover の PopoverContent)**:
  - `card-tag-add-popover.tsx:239-240` の `<PopoverContent className="w-auto max-w-sm p-0">` を以下に変更:
    ```tsx
    <PopoverContent
      className="min-w-56 max-w-sm p-0"
      onEscapeKeyDown={...}
    >
    ```
    - `w-auto` を**削除**、 `min-w-56` を追加 (`min-w-56` = 14rem = 224px、 Tailwind v4 既定)
    - `max-w-sm` 維持 (= 24rem = 384px)
    - `p-0` 維持
  - `card-tag-edit-popover.tsx:119-120` も同様に `<PopoverContent className="w-auto max-w-sm p-0">` → `<PopoverContent className="min-w-56 max-w-sm p-0">`
  - `onEscapeKeyDown` 含む他の props 一切無改修
  - 効果 (Step 0 + spec §設計判断 1 で確認済):
    - stage A (category combobox) → min-w-56 floor 224px
    - stage B (option combobox、 新規 category 作成直後 empty placeholder 表示) → max-w-sm cap 384px、 placeholder は 2-3 行 wrap
    - stage C (型選択 2 button) → min-w-56 floor 224px
    - stage D (既存 category 編集 option list) → min-w-56 floor 224px
    - → stage A / C / D で popover 幅が**揃う**、 stage B のみ 384px (本 sprint scope 内では受容、 別 sprint で empty placeholder 短文化検討)

- **Fix-3 (型選択 stage 2-button outer)**:
  - `card-tag-add-popover.tsx:510` の `<div className="px-2 pb-1">` を以下に変更:
    ```tsx
    <div className="pb-1">
      <button ... className="flex w-full items-center gap-2 px-2 py-1.5 text-sm hover:bg-slate-100 rounded">
        <CircleDot ... />
        <span>シングルセレクト</span>
      </button>
      <button ref={multiButtonRef} ... className="flex w-full items-center gap-2 px-2 py-1.5 text-sm hover:bg-slate-100 rounded">
        <CheckSquare ... />
        <span>マルチセレクト</span>
      </button>
    </div>
    ```
    - outer の `px-2` を**削除**、 `pb-1` のみ残す
    - button class (`flex w-full items-center gap-2 px-2 py-1.5 text-sm hover:bg-slate-100 rounded`) は完全維持
    - 効果: button content 左端が popover 左から **16px (outer 8px + button class 8px) → 8px (button class のみ)** に移動、 他 stage の row content と縦揃え一致
  - back button block (line 485, `<div className="px-2 pt-2">`) は無改修 (既に 8px インデント、 修正不要)
  - createError `<p>` (line 532, `className="px-2 text-xs text-red-600"`) は無改修 (既に 8px インデント)
  - `handleConfirmType` / `multiButtonRef` declaration / default focus useEffect / 二重発火 guard / Esc handler / popover close reset / `pendingCategoryName` state / `isSubmittingCreate` state 完全無改修

- 両 fix とも source code 修正は**極小**: PopoverContent class 1 文字列 × 2 file + 型選択 outer class 1 文字列 × 1 file = 計 3 箇所の class 修正のみ

**完了条件:**

- `card-tag-add-popover.tsx` PopoverContent class に `min-w-56` が含まれ、 `w-auto` が含まれない (grep verify)
- `card-tag-edit-popover.tsx` PopoverContent class 同様 (grep verify)
- `card-tag-add-popover.tsx:510` の 2-button outer class が `pb-1` (`px-2 pb-1` ではない)、 grep verify (`px-2 pb-1` 文字列が production code から消滅)
- test:
  - PopoverContent class assertion (`min-w-56` 含む、 `w-auto` 不在) — 両 popover で test 追加 or 既存 test 更新
  - 型選択 stage 2-button outer の structural assertion (`pb-1` wrapper、 `px-2 pb-1` 不在) — single button の `closest('div.pb-1')` で確認、 `closest('div.px-2.pb-1')` は null
  - 既存 stage 遷移 (category → option / option → editOption / Esc 階層) / multi default focus / 二重発火 guard / createCategory 成功失敗 path / popover close reset / 同名抑制 / 全選択 focus / category 行 icon 全 regression なし
  - 既存 `<PopoverContent className="w-auto max-w-sm p-0">` を assert していた test (Tag-4b-fix / 4c-1 由来の可能性) は `min-w-56 max-w-sm p-0` に書き換え
- `pnpm test` 全件緑、 `pnpm build` 緑、 tsc strict 緑

---

### Task 3: smoke checklist + `0be3064` に amend --no-edit (push なし)

**Files:**
- Create: `docs/superpowers/plans/2026-06-09-tag-4c-2a-fix-4-smoke-checklist.md`
- (Task 1-2 の全変更 + Task 3 の smoke を `0be3064` に `git commit --amend` で畳む)

**目的:** stg 実機 smoke 観点を網羅した checklist を作り、 全変更を `0be3064` に amend で畳む (別 commit 積まず、 OT が `--force-with-lease` push)。

**制約:**

- smoke checklist file (~40-50 行) 検証カテゴリ:
  1. **Fix-1 PopoverContent min-w-56**: 両 popover (CardTagAddPopover / CardTagEditPopover) の PopoverContent が `min-w-56 max-w-sm p-0` class (DevTools Elements で確認)、 stage A / C / D で popover 横幅が**揃う** (実機目視 ±4px 以内、 概ね 224px)
  2. **Fix-1 stage B**: 新規 category 作成直後の option list (empty placeholder 表示) で popover が max-w-sm 384px に張りつき、 placeholder が 2-3 行 wrap (受容範囲、 別 sprint で text 短文化検討の余地)
  3. **Fix-2 break-all**: 長 Japanese option 名 (例「肺気腫合併心房細動」) を持つ category で option 一覧表示時、 color pill 内で名前が wrap (max-w-sm 384px 内に収まる)。 popover 横幅は max-w-sm 超えない
  4. **Fix-2 新規作成行**: 長 Japanese 名を input に入れたとき新規作成行が wrap、 popover 横幅は max-w-sm 超えない
  5. **Fix-3 型選択 stage 二重 px-2 解消**: 型選択 stage (createCategoryType) の 2 button (シングル / マルチセレクト) の content 左端が popover 左から 8px (stage A / B / D の row content と縦揃え一致、 DevTools Computed で確認)
  6. **挙動 regression**: multi default focus + Enter 即決定 / 二重発火 guard / Esc 階層 / popover close reset / category 同名抑制 / 編集 stage 全選択 focus / category 行 select_type icon 全て regression なし
  7. **console error 0** + Tag-4c-1 / 4c-2a / 4c-2a-fix / 4c-2a-fix-2 / 4c-2a-fix-3 全 regression なし

- amend 手順 (Claude Code が実行、 push しない、 Tag-4c-2a-fix-3 Task 3 と同 pattern):
  1. `git status` で working tree 確認 (Task 1-2 source/test 変更 + Task 3 smoke checklist が未 commit、 HEAD の直近 4 commit は docs commit のはず: fix-4 docs (本 sprint で先に作成済) + fix-3 + fix-2 + fix)
  2. `git log --oneline -6` で amend target `0be3064` の位置確認 (HEAD~4)
  3. `git reset --soft HEAD~4` で直近 4 docs commit を pop → HEAD が `0be3064` に戻る、 8 docs file が index に staged で残る、 Task 1-2 source/test 変更は working tree に残る
  4. `git restore --staged` で 8 docs file を unstage:
     ```bash
     git restore --staged docs/superpowers/specs/2026-06-09-tag-4c-2a-fix-4-design.md \
                          docs/superpowers/plans/2026-06-09-tag-4c-2a-fix-4.md \
                          docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-3-design.md \
                          docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-3.md \
                          docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-2-design.md \
                          docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-2.md \
                          docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-category-combobox-design.md \
                          docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-category-combobox.md
     ```
  5. Task 1-2 source/test + Tag-4c-2a-fix-4 smoke を add:
     ```bash
     git add 'app/(app)/app/exams/[id]/_components/card-tag-add-popover.tsx' \
             'app/(app)/app/exams/[id]/_components/card-tag-add-popover.test.tsx' \
             'app/(app)/app/exams/[id]/_components/card-tag-edit-popover.tsx' \
             'app/(app)/app/exams/[id]/_components/card-tag-edit-popover.test.tsx' \
             'app/(app)/app/exams/[id]/_components/card-tag-option-list.tsx' \
             'app/(app)/app/exams/[id]/_components/card-tag-option-list.test.tsx' \
             docs/superpowers/plans/2026-06-09-tag-4c-2a-fix-4-smoke-checklist.md
     ```
  6. `git commit --amend --no-edit` で `0be3064` を amend (Task 1-2 + smoke を取り込む、 message 維持)
  7. 4 docs commit を**元の順序 + 元の message** で再 commit (Tag-4c-2a-fix → 4c-2a-fix-2 → 4c-2a-fix-3 → 4c-2a-fix-4 の順):
     ```bash
     # Tag-4c-2a-fix docs
     git add docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-category-combobox-design.md \
             docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-category-combobox.md
     git commit -m "docs(tag): Tag-4c-2a-fix spec + plan (stage1 を category combobox に統一 + 型選択 stage 導入) [no-review]"

     # Tag-4c-2a-fix-2 docs
     git add docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-2-design.md \
             docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-2.md
     git commit -m "docs(tag): Tag-4c-2a-fix-2 spec + plan (型選択 stage コンパクト化 + category 同名抑制 + 編集 stage 全選択 focus) [no-review]"

     # Tag-4c-2a-fix-3 docs
     git add docs/superpowers/specs/2026-06-08-tag-4c-2a-fix-3-design.md \
             docs/superpowers/plans/2026-06-08-tag-4c-2a-fix-3.md
     git commit -m "docs(tag): Tag-4c-2a-fix-3 spec + plan (幅揃え + 見出し削除 + multi icon CheckSquare + category 行 select_type icon) [no-review]"

     # Tag-4c-2a-fix-4 docs
     git add docs/superpowers/specs/2026-06-09-tag-4c-2a-fix-4-design.md \
             docs/superpowers/plans/2026-06-09-tag-4c-2a-fix-4.md
     git commit -m "docs(tag): Tag-4c-2a-fix-4 spec + plan (popover 幅一貫性 min-w-56 + break-all + 型選択 二重 px-2 解消) [no-review]"
     ```
     - 各 message に `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` を付与 (既存 pattern)
  8. `git log --oneline -6` で 5 commit (amend 後 feat + 4 docs) を確認

- push 手順 (Claude Code は実行しない、 OT が後続で実行):
  - `git push --force-with-lease origin develop`

**完了条件:**

- smoke checklist file 作成 (~40-50 行、 7 検証カテゴリ)
- `pnpm test` 全件緑、 `pnpm build` 緑、 tsc strict 緑
- `git commit --amend --no-edit` 実行後、 working tree clean、 Task 1-2 + Fix-4 smoke が `0be3064` 後継 (新 hash) に統合済
- 4 docs commit (Tag-4c-2a-fix + 4c-2a-fix-2 + 4c-2a-fix-3 + 4c-2a-fix-4) が新 hash で再積み済、 元の順序 + 元の message 維持
- push は **実行しない** (停止、 OT smoke 後に OT が force-with-lease push)

---

## Plan 完了後の OT smoke (push 前停止後)

OT 確認: smoke checklist の検証カテゴリを順次 PASS / FAIL で記録。 特に:
- stage A / C / D で popover 横幅が体感で揃うか (DevTools 実 px ±4px 以内)
- stage B (新規 category 作成直後 option list) で popover が 384px 張りつき + placeholder 2-3 行 wrap が受容範囲か (別 sprint で text 短文化の要否判断)
- 長 Japanese 名 (実機で OT が用意) が color pill で wrap、 popover が max-w-sm 超えないか
- 型選択 stage の 2 button content 左端が他 stage の row content (8px) と縦揃え一致か

smoke 確認 OK の後、 OT が `git push --force-with-lease origin develop` を実行。

行数: ~232 行 (300 cap 内、 目安 150-250 内)。 3 fix を 3 task に分割 (Task 1 = option-list、 Task 2 = popover 両 file の class 修正、 Task 3 = smoke + amend)、 smoke checklist 別 file 化で本体圧縮。
