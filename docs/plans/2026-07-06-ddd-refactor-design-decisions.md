# DDD リファクタ top-level design decisions

- 日付: 2026-07-06 / branch: `dddrefactor`(develop `8b4a1e9` から分岐)
- 根拠調査: `docs/audit/2026-07-05-ddd-refactor-investigation.md`(統合版・現状検証済。§ 参照は同 doc)
- 本 doc の役割: **プログラム全体の確定判断と phase 進捗の SSoT**。各 phase の実装詳細は持たない(phase ごとの spec / plan が持つ)。仕様判断が変わる場合は本 doc を更新してから phase spec に反映する。

---

## 1. 確定判断(OT 確定・2026-07-06)

### D-1. pragmatic DDD

domain 純粋層 + use-case 関数 + 既存 seam 昇格。フル DDD(全 aggregate に entity クラス + repository の機械的導入)はしない。
**client 側は既存 `runOptimistic*` を application service に昇格し、新 repository 層は作らない**(mirror 書込 + outbox enqueue + flush kick は persistence でなく application transaction であり、repository 抽象はこれを不可視化するため)。

### D-2. wire 変更系はスコープ外 + 凍結契約の定義

audit §6.2(pull の wire generic 化 / outbox 2 系統統合 / 競合解決統一)は今回やらない(→ §3)。
**凍結対象 = 「挙動同一の契約」**: API payload shape / Dexie schema※ / entity_mutations 形式、に加えて **error code・HTTP status・user-facing 日本語文言・cache header・revalidatePath 対象・tombstone entity_type・op 名・ops/log イベント名**。全 phase の review 観点に含める。
※ Dexie schema は D-6 により data 喪失観点の凍結は解除。ただし契約としての形(store 名・index・型)の変更は別 commit 隔離(D-6)。

### D-3. 安全網 = contract/golden test を標準

P0 で /api/pull response・mutation envelope・review-events bulk result・upload result union・webhook 状態遷移の snapshot を固定し、以降の全 phase の回帰検知の正とする。Playwright/E2E は**任意**(必要と判断した時点で別途 OT 相談 — 新規依存のため)。

### D-4. P0 から sprint 化

全 phase の安全網が P0 で作られるため先頭が正しい。phase 順は P0 → P1 → P2 → P3 → P4(§2)。

### D-5. 着手時期 = 今

dddrefactor branch 上で開始。exams UI への機能追加が進むほど P3 対象が広がるため先送りしない。

### D-6. DB 全消去可(prod 含む)の反映

- **Dexie 移行コード・lazy migration・旧版 schema 保持は書かない**。Dexie 変更は version bump + 旧定義削除で行う。
- Dexie store/index 変更は data 喪失リスクとしては**解除**(audit §6.2 の「pending outbox 喪失リスク」は前提消滅)。
- ただし **Dexie schema 変更は DDD 抽出(コード移動・層再編)とは別 commit に隔離**する — bisect 可能性の維持(「移動で壊れたのか schema で壊れたのか」を切り分け可能に保つ)。
- **隔離対象は「形の変化」に限定**:
  - store の追加・削除・index 変更 = D-6 隔離対象(別 commit)。
  - 既存 store の中身削除のみ(store 名・index・型は不変)= 通常の抽出 commit に同居可。
  - 理由 = bisect の切り分け対象は「schema の形が変わったか」であり、データの有無ではない(全消可のため)。形が不変なら「移動で壊れた」以外の疑いが無く隔離不要。

---

## 2. Phase 進捗表

定義は audit §7 の骨子(1 行要約)。詳細は各 phase spec が持つ。

| Phase | 内容(1 行) | 状態 | 完了時 HEAD | 再スキャン記録 |
|---|---|---|---|---|
| P0 | contract/golden tests + smoke checklist + import 境界 lint(allowlist 方式)+ dead code・stale 掃討 | 完了(SHA 記録済・OT push 待ち) | 7b36e58(実装完了)+ 本 SSoT gate commit | 現 HEAD 全数再スキャン済(§A triage 16 乖離 = intentional 15 / 対象外 1 / bug 0)。deliverable = docs/audit/2026-07-06-p0-contract-baseline.md(§A triage + §B 凍結契約 inventory)/ 2026-07-06-p0-smoke-checklist.md。contract golden = tests/contract/(5 面 77 test)。lint 境界 = eslint.config.mjs + tests/lint/import-boundary.test.ts。gate: whole-repo lint --max-warnings=0 / typecheck / test(2979)/ test:contract(77)全 exit 0 |
| P1 | domain 純粋層の抽出・移設 + 二重実装の仕分け・単一 source 化 | 完了(SHA 記録済・OT push 待ち) | 8a0e8ee(実装完了・Task6)+ 本 SSoT gate commit | 着手時 `a11afca` 再スキャン済(§5.2 純粋資産は既に lib/ 適所・逆流ゼロ / carve-out 2 + 型 relocate 1 + 逆依存 1 + dedup 2 が実体)。**Task1** compareTagEntry 抽出(3 site dedup)**Task2** computeStreak+addDays→lib/streak-core.ts hoist(二段構え温存)**Task3** deriveExamStatuses→lib/exams/derive-exam-statuses.ts carve(pure import0)**Task4** classifyChange+getPendingState→lib/stripe/subscription-changes.ts carve(決済・import type Stripe のみ・build server-only 混入なし)**Task5** card-filter-predicates→lib/cards move + allowlist entry 削除=**P0 lint 機構初回実証**(削除後 lint green で逆依存消滅実証)**Task6** CustomSessionCriteria→type-only module relocate(pure seed の Dexie 型 edge 切断)**Task7** V5 filter 代数 confirm-only=単一 source 確定(predicate/型が card-filter-predicates.ts に単一・3 重コピーなし)。全 code task = canonical review pass、Task3/4/5 = Codex 独立 review pass(全 Crit0/Imp0)。**最終 gate 全 exit 0**: test 2983(P0 2979+compareTagEntry unit 4)/ typecheck / whole-repo lint --max-warnings=0 / build。**残リスク**: source-doc-status.ts の 3 DB 関数は既存 unit test なし(P1 挙動不変ゆえ非追加・route.test が経路担保)。**latent 不純**(fsrs.rate/newCard/todayInJst の =new Date() default)は P1 非対象・baseline §B 申し送り(clock 注入 phase で扱う)。spec = specs/2026-07-07-ddd-p1-domain-extraction-design.md / plan = plans/2026-07-07-ddd-p1-domain-extraction.md / Codex = codex/2026-07-07-plan-ddd-p1-domain-extraction.md + task3/4/5-*.md |
| P2 | server 側 use-case 化(review-events route → webhooks 2 本 → process.ts 分解) | 完了(SHA 記録済・OT push 待ち) | fdb7d93(実装完了・Task5)+ 本 SSoT gate commit | 着手時 `b3bcb07` 再スキャン済(audit §3.3/§6.1 の 4 対象 594/441/417/761 行・全一致・stale なし)。**Task1** processSession+schema+deriveRating→lib/reviews/ingest-review-events.ts(単一 tx + count mismatch 防御 as-is・route は Phase 0 session upsert + measure + auth + parse + response 残置)**Task2** handleEvent 一式→lib/stripe/handle-stripe-event.ts(status matrix 非対称・release gate・error 200 swallow 分担不変)**Task3** handleEvent/handleUserDeleted 一式→lib/clerk/handle-clerk-event.ts(新 dir・10 DELETE 単一 tx・cancel ループ tx 外・retry helper 同居温存・stale コメント 8→10 回収)**Task4** guard tx→_actions/upload-guard.ts(advisory lock〜sourceDoc INSERT 単一 tx・meta.* rewiring のみ)**Task5** 保存 tx/完了 tx/markFailed→_actions/upload-persistence.ts(applyOcrTags 同 tx 採番維持・customProps[i] index 対応)。全 code task = canonical review + Codex 独立 review pass(全 Crit0/Imp0)。**最終 gate 全 exit 0**: test 2983 / typecheck / whole-repo lint --max-warnings=0 / test:contract 77 / build。**whole-branch review(opus・16938e8..fdb7d93)= READY TO MERGE / Crit0 Imp0 Minor0**。measure/timing 配管温存(TEMP marker revert は別 task・OT 判断)。spec = specs/2026-07-07-ddd-p2-server-usecase-design.md / plan = plans/2026-07-07-ddd-p2-server-usecase.md / Codex = codex/2026-07-07-plan-ddd-p2-server-usecase.md + task1〜5-*.md |
| P3 | client 側 use-case 化(タグ CRUD 移設 / card write 集約 / side peek 複製解消 / runOptimistic* 昇格 / inline primitive 統合) | 完了(SHA 記録済・OT push 待ち) | 4a89017(実装完了・Task8)+ 本 SSoT gate commit | 着手時 `aeb4d23` 再スキャン済(audit 主張ほぼ全数一致・乖離 2 = V3 正規化は 1 箇所実体〈分散集約に補正〉/ 「安全網最薄」は components/ の数字で P3 対象 11 file は co-located test 現存〈characterization は監査+差分追加の 2 段に再定義〉)。subagent-driven で 10 task(Task0 characterization 先行 + Task1-8 code + Task9 gate)完走。**Task0** 地雷 gap 4 pin(selection prune / TagCell override / delete reject / cross-component 統合 flow)**Task1** タグ CRUD 12 関数 + TagEditCallbacks を lib/tags/tag-crud.ts へ verbatim 移設(V1 解消)**Task2** 直 tx 6 箇所 → runOptimistic* helper(reject 伝播は throwOnError 保存・log 制約 Option B〈sibling *.tx_failed 命名内 error-path warn 許容〉)**Task3** tags manager cascade delete を lib/tags use-case へ単一 source 化(delete 本体 byte-equivalent・UI 状態 caller 残置)**Task4** card write domain rule を lib/cards/card-write.ts へ pure carve(react-hooks/refs off 据え置き)**Task5** side peek 複製解消(縮小 fallback = verbatim tail のみ card-editor-fields.tsx 抽出)**Task6** inline primitive 低次共有 _lib/inline-edit-shared.ts(dirty-guard は挙動発散で縮小)**Task7** Tier-3 dead 掃討(createOptionAndAssign optional 化 + placeholder/onOpenEdit 除去)**Task8** import 境界回収(深相対 alias 化 + cross-feature 4 allowlist)。全 code task canonical review pass、risk task(Task1/2/3/5)Codex 独立 review pass(全 Crit0/Imp0)。**whole-branch review(opus・a7ab150..631f510)= READY TO MERGE / Crit0 Imp0 Minor4**(全 Minor 非 defect: ActionBar guard=false alarm〈bulkCreateOptionAndAssign 存続で guard 非発火・controller 独立検証〉/ logContext 正規化 / console text 非凍結 / session-runner コメント=Task7 明示 scope)。**最終 gate 全 exit 0**: full test 3004 / typecheck / whole-repo lint --max-warnings=0 / test:contract 77(**snapshot 更新ゼロ=契約不変証明**)/ build。詳細 = sessions/2026-07-07-ddd-p3-factfinding.md / specs/2026-07-07-ddd-p3-client-usecase-design.md / plans/2026-07-07-ddd-p3-client-usecase.md / codex/2026-07-07-ddd-p3-task{1,2,3,5}.md |
| P4 | インフラ DRY(outbox flush 層の限定共通化 / pull server 内 factory / retry・transient 統合 / 認証 wrapper / lib 再編) | spec 起草中 | — | 着手時 `a62fb1c` 再スキャン済(Explore 6 体 + controller 裏取り・OT 承認)。**乖離 4 点で P4 縮小**: outbox sanctioned scope(withWebLock / classifyFlushResults 共有)= 消化済・残余 per-table 同型 5 対のみ / retry も ocr・review-flush は lib/retry 共有済・残余 clerk local + dir 並列 / route 認証 = read-only 4 ⇄ mutation 2 の 2 形態 / pull 6 module 同型は audit どおり。詳細 = sessions/2026-07-07-ddd-p4-factfinding.md |

**運用注記**:

- 各 phase は sprint フロー(brainstorming → spec → plan → 実装 + canonical/Codex review)に載せる。phase 完了時に本表へ **完了時 HEAD SHA** と **再スキャン箇所**(着手時に audit の該当主張を現 HEAD で再確認した範囲と結果)を記録する。
- audit の file:line は `5d3baef` 検証時点。**phase 着手時に対象箇所を再スキャン**し、stale があれば audit でなく phase spec 側に現状を記す(audit は歴史記録として凍結)。
- 状態欄の値: 未着手 / spec 起草中 / plan 確定 / 実装中 / 完了(SHA 記録済)。
- **状態欄の更新は各 phase の CC が該当 commit と同じ commit で行う**(spec 起草開始時 → spec 起草中、plan 確定 commit 時 → plan 確定、実装 commit 時 → 実装中、完了時 → 完了 + HEAD SHA 記録)。OT push で確定する。

---

## 3. やらない 4+1(理由の記録)

後から「なぜやらなかったか」を追うためのセクション。解除には OT 判断を要する。

| # | やらないこと | 理由 |
|---|---|---|
| N-1 | **競合解決 3 方式(replay / LWW / server-wins)の統一** | **反 DDD だから**。FSRS=event replay、entity 編集=LWW、mirror=server-wins は各 context のドメイン特性に合った正しいモデルであり、統一は「見た目の一貫性」のために意味論を壊す。恒久にやらない(deferではない)。 |
| N-2 | **outbox 2 系統(entity_mutations ⇄ answer_events)の統合** | **defer**。価値小: review 系のみ retry controller / pullBack hook / session grouping を持つ非対称(検証済)で、統合は大改修の割に得るものが薄い。将来 3 系統目が必要になった時に再評価。 |
| N-3 | **pull の完全 generic 化(PullResponse 型そのものの wire generic 化)** | server 内 factory 化(wire に出ない範囲)は **P4 で in scope**。wire generic 化は残り 2 割の**任意 consider** — P4 spec 起草時に費用対効果を見て判断し、採らない場合は理由を P4 spec に記す。 |
| N-4 | **Dexie schema の再設計(store 分割・index 全面見直し等)** | **別 sprint**。DDD 層再編と直交する関心。D-6 により移行コスト自体は低いが、混ぜると bisect 可能性と review 粒度を壊す。本リファクタ中の Dexie 変更は「抽出に必要な最小限 + 別 commit 隔離」のみ。 |

**dead code の Tier 分散(P0 spec 由来・2026-07-06)**: audit §4.3 の dead リストは「P0 で全消し」ではなく phase 分散に変更(現 HEAD 再スキャンで多くが「本体は内部利用で生存・export だけ test 専用」型 or 型連鎖の設計変更と判明)。Tier 1(完全 dead + stale コメント)= P0 / Tier 2(export-only-dead: isUpgrade・newCard・buildNewOption・jstMonthBoundsUtc・scheduler)= owning phase / Tier 3(onOpenEdit・createOptionAndAssignPlaceholder)= P3。詳細表 = P0 spec §6。後 phase での「掃討し忘れ」誤認防止のための記録。
| N-5 | **client 側 repository 層の新設**(= D-1 の裏面) | **唯一の pragmatic 判断点として明示**。教科書的 DDD なら repository を置く場面だが、Dexie mirror + outbox + flush は application transaction であり、隠蔽すると coalesce / rollback / pull-back の同期挙動が不可視化する(audit §5.3、Codex 指摘 3)。`runOptimistic*` の application service 昇格で代替する。 |

---

## 4. 変更履歴

- 2026-07-07: **P4 spec 起草開始**(状態 → spec 起草中)。起草 = Fable(P3 完了時 OT 判断)、実装 = CC(Opus)subagent-driven。fact-finding(現 HEAD `a62fb1c`・Explore 6 体 + controller 裏取り)を OT 承認 — 乖離 4 点で P4 は「取り残された小さな DRY + lib 整理 + TEMP 撤去」に縮小。OT 確定 5 論点 = ① outbox 残余 = per-table 同型 5 対の helper 化(限定共通化 ≠ 統合・非対称部不触で N-2 維持)② measure revert 実施(consumer 不在 + TEMP 設計通り撤去の 2 条件を fact-finding で確認済・D-2 の sanctioned 例外として spec 記録)+ contact-form → lib/actions 移設 + PullResponse wire generic 不採用(N-3 見送り確定・理由 spec 記録)③ clerk local runTransactionWithRetry 現状維持(YAGNI)+ retry⇄transient dir 配置整理のみ ④ route wrapper = read-only 4 本のみ(mutation 2 本不触)⑤ lib 再編 = 並行命名 2 組解消 + replacer 統合 + VERCEL_ENV helper 化 / toISOString nowIso 化は clock 注入 phase 送り。spec = specs/2026-07-07-ddd-p4-infra-dry-design.md(未決 1 = CLAUDE.md の lib/clerk.ts・lib/stripe.ts path 名指し 2 行の扱い — User Review Gate で OT 判断)。
- 2026-07-06: 初版(OT 確定判断 D-1〜D-6 / phase 表初期化 / やらない 4+1)。
- 2026-07-06: D-6 に Dexie 隔離対象の明確化(形の変化に限定)、進捗表に状態更新の責任者・タイミングを追記。
- 2026-07-06: P0 spec 起草開始(状態 → spec 起草中)。dead code Tier 分散を §3 に記録(P0 spec §6 詳細)。
- 2026-07-06: **P0 完了**(状態 → 完了)。subagent-driven で 11 task 実走(T0 triage→T1 fixtures→T2-T6 contract golden 5 面→T7 import 境界 lint→T8 dead-sweep Tier1→T9 §B+smoke→T10 gate)。各非自明 task に canonical(requesting-code-review)+ Codex 独立 review 実施・全 Critical/Important 収束。bug handoff(条件 2)不発火(§A bug 0)。実装完了 HEAD=7b36e58。
- 2026-07-07: **P1 spec 起草開始**(状態 → spec 起草中)。前セッションの brainstorming(Explore ×3 で現 HEAD `a11afca` 実測)+ OT 承認済み方向を spec 化。3 判断点 = 物理構造(lib/domain 一括新設せず carve-out のみ)/ 逆依存 P1 解消(card-filter-predicates 移動 = P0 lint 機構初回実証)/ streak characterization 新規不要(既存二重 suite re-point)。P1 実体 = carve-out 2(deriveExamStatuses / classifyChange+getPendingState)+ 型 relocate 1(CustomSessionCriteria)+ 逆依存解消 1 + dedup 2(computeStreak hoist / compareTagEntry 抽出)。V5 filter 代数は確認のみに格下げ。latent 不純は §B 申し送り。
- 2026-07-07: **P1 plan 確定**(状態 → plan 確定)。8 task(Task1 compareTagEntry / Task2 streak hoist / Task3 deriveExamStatuses carve / Task4 subscription pure carve〈決済〉/ Task5 card-filter-predicates move + allowlist 削除 / Task6 CustomSessionCriteria relocate / Task7 V5 confirm / Task8 最終 gate + SSoT)。Codex plan cross-check(1 パス)反映済。OT 補正 = review 粒度分離(canonical 全 task / Codex+build は Task3/4/5 のみ / full test は Task8 集約)。共有 module = lib/streak-core.ts。SSoT commit は code と分離。次 = subagent-driven で Task1 自走(Critical 未解決 or build server-only 混入時のみ停止)。
- 2026-07-07: **P1 完了**(状態 → 完了)。subagent-driven で 8 task 完走(Task1-6 code / Task7 confirm / Task8 gate)。実装 = fresh subagent(commit させず controller が review→[reviewed] commit の一方向則)。canonical(SDD task-reviewer)全 code task pass、Codex 独立 review = risk task Task3/4/5 pass(全 Crit0/Imp0)。per-task Minor は全て controller 即修正(stale comment / dead re-export / import style 統一)。build は Task3/4/5 + 最終 Task8 集約で全 exit 0(Task4 決済 carve-out で server-only 混入なし=escalation 不発火)。最終 gate: test 2983 / typecheck / lint / build 全 exit 0。実装完了 HEAD=8a0e8ee。Critical 停止・build 混入・仕様揺れとも不発火で一気通貫自走。次 = OT 報告 → push → stg smoke(OT 指示)。P2 は別 sprint 境界で停止。
- 2026-07-07: **P2 完了**(状態 → 完了)。subagent-driven で 6 task 完走(Task1-5 code / Task6 gate + docs)。実装 = fresh subagent(commit させず controller が review→[reviewed] commit の一方向則)。全 code task で canonical(SDD task-reviewer)+ Codex 独立 review pass(全 Crit0/Imp0)。per-task Minor は全て record-only(移動で stale 化した行番号コメント修正 1・verbatim body への非 sanctioned 追加提案 2・組織的観察 2 — いずれも非 defect or verbatim 規律優先で非修正)。route 3 本 wire 境界化 + use-case as-is 移動(lib/reviews 新設 / lib/stripe 追加 / lib/clerk 新設)、process.ts guard/persistence を _actions 配下 2 module へ in-place 分解(1 関数=1tx・applyOcrTags 同 tx 採番維持)。全 gate exit 0(test 2983 / typecheck / whole-repo lint --max-warnings=0 / test:contract 77 / build)。**whole-branch review(opus)= READY TO MERGE / Crit0 Imp0 Minor0**。P0 golden 全面 green(snapshot 更新ゼロ=契約不変の証明)。measure/timing log 接触・golden 赤・tx 境界不能 とも不発火で一気通貫自走。実装完了 HEAD=fdb7d93。次 = OT 報告 → push → merge 後まとめ stg smoke(baseline §B(vi))。P3 は別 sprint 境界で停止。
- 2026-07-07: **P2 plan 確定**(状態 → plan 確定・OT 承認 `f168b9c`)。6 task(Task1 review-events→lib/reviews / Task2 stripe webhook→lib/stripe / Task3 clerk webhook→lib/clerk〈+stale コメント 8→10〉/ Task4 process.ts guard 分解→_actions/upload-guard / Task5 persistence 分解→_actions/upload-persistence / Task6 最終 gate + SSoT + baseline §B(vi))。Codex plan cross-check 1 パス反映(採用 15 = tx 内 I/O は tx 経由・ParsedEvent type export・server-only・mock 到達性列挙・保存 tx index invariant 等 / 不採用 3 = per-task gate 軽量化・guard/persistence lib 化・Phase 0 lib 寄せ〈全て OT 方針と衝突〉)。OT 反映 2 条件 = Phase 0 境界順序(Task1 制約・「wire 境界のみ残す」唯一の明文例外)/ measure・timing log 接触 = Critical 相当で自走停止(Global エスカレーション)。review 粒度 = 全 code task で canonical + Codex + per-task build、full test は Task6 集約。次 = subagent-driven で Task1 自走(Global エスカレーション条件のみで停止)。
- 2026-07-07: **P2 spec 起草開始**(状態 → spec 起草中)。起草 = Fable(P1 完了時 OT 判断・handoff 準拠)、実装 = CC(Opus)。現 HEAD `b3bcb07` 再スキャン(4 対象全一致)+ OT 方向承認済み 5 論点(①配置 = lib/reviews 新設 / lib/stripe 既存 / lib/clerk 新設〈stripe 対称〉②process.ts は in-place 分解〈現 path 維持必須・lib/upload 新設なし〉③as-is 移動のみ〈measure 配管温存〉④新規 unit test なし〈route.test + golden が正〉⑤clerk stale コメント 8→10 回収)+ 追加 4 条件(lib/reviews は暫定置き場注記 / 1 関数 = 1 tx〈lock も tx も跨がない〉/ mock 境界の task ごと検証 / golden 赤 = 即停止)を spec 化。
- 2026-07-07: **P3 完了**(状態 → 完了)。subagent-driven で 10 task 完走(Task0 characterization 先行 / Task1-8 code / Task9 gate + docs)。実装 = fresh subagent(commit させず controller が review→[reviewed] commit の一方向則)。canonical(SDD task-reviewer・sonnet)全 code task pass、Codex 独立 review = risk task Task1/2/3/5 pass(全 Crit0/Imp0)。per-task Minor は全て controller 即修正(mock stale prose / put→add prose / closure wrapper / コメント明確化等)。**P0〜P2 と決定的に異なる点**: P3 は client interaction を P0 golden が cover しないため、Task0 で characterization 4 pin(地雷 gap #4/#5 + delete reject + cross-component 統合 flow)を先行構築し回帰網を自前担保。characterization 戦略は fact-finding の乖離 B(「安全網最薄」は components/ の数字・P3 対象 11 file は co-located test 現存)により「監査 + 差分追加」の 2 段に再定義(既存 6/8 pin 済 → 欠落 2 + 統合 flow 1 のみ追加)。**自走中の OT 停止 1 回** = Task2 の log 制約衝突(helper が throwOnError でも必ず logger.warn)→ OT が Option B 確定(sibling 命名内 error-path warn 許容・shared helper 非改変)で自走再開。地雷 8 invariant は全 task で保持(単一 subscription / commit-on-unmount 非対称 / V6 blur→close / memo 凍結等)。**whole-branch review(opus・a7ab150..631f510)= READY TO MERGE / Crit0 Imp0 Minor4**(全 Minor 非 defect・#1 ActionBar guard は controller 独立検証で false alarm 確定)。最終 gate: full test 3004 / typecheck / whole-repo lint --max-warnings=0 / test:contract 77(snapshot 更新ゼロ)/ build 全 exit 0。実装完了 HEAD=4a89017。次 = OT 報告 → push → merge 後まとめ smoke(baseline §B(vi)、E2E 残余 3 点含む)。P4 は別 sprint 境界で停止。
- 2026-07-07: **P3 spec 起草開始**(状態 → spec 起草中)。起草 = Fable(phase 表当初からの確定)、実装 = CC(Opus)subagent-driven。fact-finding(現 HEAD `aeb4d23` 再スキャン・Explore 4 体 + controller 裏取り)を OT 承認。乖離 2 件を spec 前提に反映: (A) V3 = 正規化 3 重複でなく「patch 構築 + 正規化 + commit 経路の 3 file 分散」の所在整理 / (B) characterization 戦略 = 「ゼロから構築」でなく「(a) 地雷 invariant × 既存 test の対照監査 → (b) 欠落 invariant + cross-component flow のみ追加」の 2 段に再定義(P3 対象 11 file に co-located test 現存・audit「~13%」は components/ の数字。P3 最難関の重心は「安全網最薄」→「地雷最密」へ)。spec の肝 = characterization 2 段 + runOptimistic* 昇格時の直 tx 6 箇所の個別判定(一律 helper 化しない・P2「二段構え非統一」と同じ判断軸)。E2E 費用対効果は spec で提示し OT 相談維持。lint 3 系統は「触る file 分だけ回収」の線引きを spec で提案。fact-finding 詳細 = sessions/2026-07-07-ddd-p3-factfinding.md。
- 2026-07-07: **P3 plan 確定**(状態 → plan 確定・OT 承認)。plan = plans/2026-07-07-ddd-p3-client-usecase.md(Task0-9)。Task0 = characterization 追加(先行・selection prune / TagCell override / delete 2 関数の reject 伝播 / cross-component 統合 flow)/ Task1 = タグ CRUD lib/tags verbatim 移設 / Task2 = 直 tx 6 箇所 helper 寄せ(throwOnError で reject 伝播保存)/ Task3 = tags manager 統一(吸収の原則 = use-case は純粋 delete のみ・UI 状態は caller 残置)/ Task4 = card write 集約(V3)/ Task5 = side peek 複製解消(縮小 fallback = field ブロックのみ共通化可)/ Task6 = inline primitive 低次共有 / Task7 = 掃討(placeholder optional 化)/ Task8 = lint 回収 / Task9 = 最終 gate + docs。Codex plan cross-check 1 パス反映(独立論点 14 = 全整合 / plan 指摘 12 = 全採用〈verbatim 差分定義・flake 対策・log 出力ゼロ条件・create 生成値同一・manager UI 状態比較・allowlist 分類コメント等〉/ 不採用 0)= codex/2026-07-07-plan-ddd-p3-client-usecase.md。OT 補正 2 = Task3 吸収の原則(delete ロジック本体差のみ停止発火)/ Task5 縮小 fallback。review 粒度 = risk task(Task1/2/3/5)canonical + Codex + build、他 canonical、full test は Task9 集約。次 = subagent-driven で Task0 自走(Global エスカレーション条件のみ停止・終着 = Task9 sprint 境界)。
- 2026-07-07: **P1 完了時 OT 判断(記録)**。① **push/merge**: dddrefactor は develop/main に反映しない — 全 phase(P0〜P4)完了 + 異常なし確認まで merge しない方針(P1 単独の develop/main 反映もしない)。develop/main 反映は **P4 完了後に OT 判断**。② **P1 個別 stg smoke = 省略**(理由: 挙動不変の pure 移設 + 既存 co-located test cover + build green + stg 環境が dddrefactor に無い)。merge 後のまとめ smoke 対象は baseline §B (vi) に申し送り。③ **最終 review Minor 2 = record-only 据え置き**(streak-core.ts の lib/ root 配置 = 判断1〈lib/domain 新設せず〉の帰結どおりで意図通り / codex md 末尾 newline = cosmetic)。
- 2026-07-07: **P3 完了時 OT 判断(記録)**。① **push/merge**: P3 も develop/main に反映しない(P1/P2 と同方針)。dddrefactor は全 phase(P0〜P4)完了 + 異常なし確認まで merge しない。**develop/main 反映は P4 完了後に OT 判断**。② **P3 個別 stg smoke = 省略**(理由: 挙動不変 + contract 77 snapshot 更新ゼロ + Task0 characterization〈2 段 = 地雷×既存 test の監査 + 欠落 gap 4 の差分追加〉で client interaction 回帰網を自前担保 + stg 環境が dddrefactor に無い)。merge 後まとめ smoke 対象は baseline §B(vi) に P3 surface + **E2E 残余 3 点**(実 focus/blur・virtualizer 実 scroll・scroll 起因 unmount commit)を操作手順+期待挙動付きで申し送り済(E2E 不採用の意図的 stg 送り)。③ **whole-branch review(opus・a7ab150..631f510)= READY TO MERGE / Crit0 Imp0 Minor4**。全 Minor 非 defect: #1 ActionBar guard = controller 独立検証で **false alarm 確定**(bulkCreateOptionAndAssign 存続〈:529 実 bulk 関数〉・P3 diff 空・guard 非発火・挙動変化ゼロ)/ #2 logContext 正規化 = 既記録・event 名凍結 / #3 handleCreateCategory console text = 非凍結・到達不能 / #4 session-runner コメント = Task7 明示 scope。④ **自走中 OT 停止 1 回(記録)** = Task2 の log 制約衝突(helper が throwOnError でも catch で必ず logger.warn→rethrow)vs 旧制約「新規 log 出力ゼロ」→ OT が **Option B** 確定(sibling `*.tx_failed` 命名内の error-path warn 許容・shared helper 非改変・「新規 log 出力ゼロ」を「既に確立した sibling パターンを超える新規 log surface ゼロ」と意図解釈)。裏取り = rename/color 4 関数は既に同パターンで reject pin pass 済 = タグ mutation 凍結 baseline に helper 由来 error-path warn が既に含まれる。plan Global Constraints + 実績欄に記録済。
- 2026-07-07: **P2 完了時 OT 判断(記録)**。① **push/merge**: P2 も develop/main に反映しない(P1 と同方針)。dddrefactor は全 phase(P0〜P4)完了 + 異常なし確認まで merge しない。**develop/main 反映は P4 完了後に OT 判断**。② **P2 個別 stg smoke = 省略**(理由: 挙動不変〈verbatim 移動 + snapshot 更新ゼロ〉+ 既存 route.test 4 本 + P0 golden が回帰担保 + stg 環境が dddrefactor に無い)。merge 後まとめ smoke 対象は baseline §B(vi) に P2 surface 追記済(演習 flush→集計 / upload OCR→preview / 決済 webhook / アカウント削除 — うち実課金・破壊的削除は OT 実機)。③ **whole-branch review(opus・16938e8..fdb7d93)= READY TO MERGE / Crit0 Imp0 Minor0**。per-task Minor は全て record-only(移動で stale 化した行番号コメント 1 = 修正 / verbatim body への非 sanctioned コメント追加提案 2 = verbatim 規律優先で非修正 / 組織的観察 2 = 非 defect)。④ **measure 計測配管(review-events TEMP marker)= P2 温存**(timing log イベント名が D-2 凍結対象ゆえ触らず)。revert 要否は **P3/P4 と独立の別 task・OT 判断**(spec §8.2)。
