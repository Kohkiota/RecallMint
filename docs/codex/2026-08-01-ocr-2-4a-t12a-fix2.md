# Codex independent review — ocr-2-4a-t12a-fix2 (2026-08-01)

- **作成日**: 2026-08-01
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

Successful publishing leaves the existing source-document lifecycle and upload ledger incomplete, producing inconsistent persisted state despite returning a published result.

Review comment:

- [P1] Finalize the source document alongside the operation — /workspaces/RecallMint/app/(app)/app/upload/_actions/publish-prepared.ts:260-266
  On every successful prepared upload, this only marks `upload_operations` completed; the associated `source_documents` row created by `prepare-upload.ts` remains `processing`, and no completed `upload_records` ledger entry is appended. Consumers that use the source-document status or upload ledger will therefore continue treating a published upload as unfinished and omit its quota/audit record. Add the guarded `completeUploadTx`-equivalent source-document update and ledger insertion to this same transaction.