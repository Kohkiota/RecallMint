# Codex independent review — ocr-2-4a-s4 (2026-08-05)

- **作成日**: 2026-08-05
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The new document-level polling response is derived from an exam-level query that discards non-latest documents. A valid completed upload can therefore become unobservable to its polling client and unnecessarily time out.

Review comment:

- [P2] Query the polled document instead of only each exam's latest — /workspaces/RecallMint/app/api/exams/status/route.ts:96-102
  Build `docStatuses` from all owner-scoped documents (or query the requested document directly), because `rows` was intentionally reduced by `DISTINCT ON (exam_id)` to one latest document per exam. If another tab uploads to the same exam after this upload finishes, this source document disappears from `docStatuses`; `pollDocStatus` treats the missing key as still pending and waits until the 20-minute degraded timeout instead of navigating to its completed result.