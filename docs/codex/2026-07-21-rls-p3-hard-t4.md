# Codex independent review — rls-p3-hard-t4 (2026-07-21)

- **作成日**: 2026-07-21
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The code and current tests pass, but later flat-config overrides create multiple production bypasses for the newly introduced repo-wide getDb restriction. The enforcement therefore does not satisfy its intended scope.

Full review comments:

- [P2] Preserve the getDb ban in domain overrides — /workspaces/RecallMint/eslint.config.mjs:334-338
  The later domain-purity blocks replace this rule and only prohibit the exact `@/lib/db` source, so a domain file can still import `getDb` through a relative path such as `../../db` or through `@/lib/db/index`. Because flat-config replacement discards `GETDB_BAN.patterns`, the stated repo-wide ban is not enforced in `lib/{stripe,reviews,cards,tags,media}/domain/**`; include these patterns in each overriding block.

- [P2] Keep the getDb restriction in per-file allowlists — /workspaces/RecallMint/eslint.config.mjs:503-508
  For the four production app files with later `no-restricted-imports: 'off'` overrides, those overrides replace this block and disable the new `getDb` ban entirely. An exact `import { getDb } from '@/lib/db'` in any of those files therefore passes lint, leaving known production paths outside the claimed repo-wide enforcement; the allowlists need to preserve the getDb restrictions while exempting only their intended boundary violations.