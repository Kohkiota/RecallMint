# Codex independent review — edit-3-fix3-t2b (2026-07-02)

- **作成日**: 2026-07-02
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The patch currently leaves the test suite failing due to stale sticky-column assertions. The new virtualization also has spacer/offset edge cases that can produce incorrect layout or row windows.

Full review comments:

- [P1] Update remaining sticky metadata tests — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-table-columns.tsx:79-84
  Removing the sticky metadata here leaves `exam-card-table-columns.test.tsx` still asserting `meta.sticky` and `stickyLeft` for `select`/`title`; the current test suite fails with those three assertions. If the intended behavior is to remove sticky columns, the remaining column-definition tests need to be updated in the same change so CI does not stay red.

- [P2] Guard spacer math when no virtual rows render — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-table.tsx:122-125
  When `virtualItems` is empty, this computes `paddingBottom` as `totalSize + scrollMargin`; with zero filtered rows this renders a blank spacer equal to the table's page offset, and when the table starts below the viewport it double-counts that offset before any rows are visible. Handle the empty virtual item case separately so an empty table has no artificial height and offscreen lists reserve only `totalSize`.

- [P2] Recompute the virtualizer offset after layout shifts — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-table.tsx:496-499
  The `scrollMargin` is measured only once on mount, but controls above the table can change height later, for example when tag filter chips are added/removed or the toolbar wraps after a resize. In those cases the virtualizer keeps using the stale table offset and can render the wrong row window while scrolling; observe/recompute the container position when the header area layout changes.