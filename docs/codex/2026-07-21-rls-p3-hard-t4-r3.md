# Codex independent review — rls-p3-hard-t4-r3 (2026-07-21)

- **作成日**: 2026-07-21
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The configured rule works for its tested directories, but it does not enforce the claimed repo-wide production restriction because root production entrypoints remain outside every new scope.

Review comment:

- [P2] Apply the getDb ban to root production entrypoints — /workspaces/RecallMint/eslint.config.mjs:345-347
  The new restriction only targets `lib/`, `components/`, `app/`, and `tests/`, so root production modules such as `instrumentation.ts`, `proxy.ts`, and `next.config.ts` can still import `getDb` without a lint error. This leaves a direct bypass in executable production code despite the stated repo-wide ban; include root production TypeScript files (and other executable source scopes such as `scripts/`, if intended) while preserving the necessary test exemptions.