# Codex independent review — ocr-2-4a-t8b-fix (2026-07-31)

- **作成日**: 2026-07-31
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The staging action can successfully persist OCR output generated from only part of the claimed source manifest, producing incomplete prepared data.

Review comment:

- [P1] Validate the complete source manifest before staging — /workspaces/RecallMint/app/(app)/app/upload/_actions/stage-prepared.ts:119-124
  Filtering to `status = 'ready'` hides missing or transitioned source rows. If one source is deleted or becomes non-ready after claim while another remains ready, this action sends only the remaining subset to Gemini and can save an incomplete payload as `prepared`; only the zero-row case is rejected later. Re-read all source rows and compare their count/status against `expectedSourceCount` before OCR, as the claim path does.