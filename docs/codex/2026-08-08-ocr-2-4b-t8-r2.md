# Codex independent review — ocr-2-4b-t8-r2 (2026-08-08)

- **作成日**: 2026-08-08
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The new source cleanup can delete shared PDF objects after a fenced terminalization loses a race, disrupting the invocation that currently owns the operation.

Review comment:

- [P1] Preserve sources when terminalization loses its fence — /workspaces/RecallMint/app/(app)/app/upload/_lib/upload-pipeline.ts:615-615
  If another invocation takes over after the start CAS but before any expected failure is terminalized, `terminalize()` returns the internal `raced` outcome, yet `ownership.lost` remains false. The outer `finally` then deletes every PDF source key, potentially causing the winning invocation's count/render GET to fail. Propagate the terminalization outcome and mark ownership lost when it is `raced`; this applies to the other new PDF terminalization calls as well.