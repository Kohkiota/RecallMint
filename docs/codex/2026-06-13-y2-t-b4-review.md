# Y-2 T-B4 #1c review raw findings (Critical 0 / Important 0 / Minor 3 — Minor 1 を実装で吸収)

- **対象**: Y-2 Sub-plan B T-B4 (#1c exam-list-live materialize 回避、 案 b)
- **review 経路**: `superpowers:requesting-code-review` skill canonical (skill template `code-reviewer.md` / general-purpose subagent / template 改変なし)
- **base / head SHA**: BASE = HEAD = `fc9210a` (commit 前 unstaged review、 CLAUDE.md「順序の絶対則 = review pass → commit ([reviewed] 込み)」 準拠)
- **変更 file 3 件**:
  - `lib/client-db.ts` (+13、 Dexie v6 schema 追加)
  - `app/(app)/app/exams/_components/exam-list-live.tsx` (+25 / -17、 per-exam count() 並列化)
  - `app/(app)/app/exams/_components/exam-list-live.test.tsx` (+58、 test #7 regression 追加)

---

## Strengths

- **Self-check 1 (filter 無し count())**: Dexie source `dist/dexie.js:1981-1984` (`isPlainKeyRange`) + `:2108-2132` (`Collection.prototype.count`) で独立検証、 `ctx.filter / ctx.algorithm / ctx.or` が全て falsy → `ignoreLimitFilter=true` 経路 → `coreTable.count({range})` 直送 → native `IDBIndex.count(IDBKeyRange)` 経路に確実に乗る。 materialize 0 の構造保証が **コード構造レベル** で成立。
- **Self-check 2 (owner isolation)**: index 第 1 要素 `userId` を `equals([userId, e.id])` で fix → 他 user の cards に IDB index 経路で構造的に到達不能。 既存 test #6 が functional に成立を証明。
- **Dexie schema 純粋追加 (v6)**: v2 / v4 / v5 と同形 (store drop / column 変更 / upgrade callback すべてなし)。 v1-v5 ブロックを一切触らない最小 diff。
- **コメントの「なぜ」原則**: `exam-list-live.tsx:35-44` の 4 bullet は CLAUDE.md「コメントは『なぜ』 のみ」 に合致 — 後続 task が `.filter()` を足す事故を防ぐ防波堤として機能。
- **テスト #7 の妥当性**: `vi.spyOn(db.cards, 'where')` で「実 IDB (fake-indexeddb) 上の実 behavior + 引数 tracking」 = mock ではなく実走。 既存 #1 / #6 の functional 検証と組合せて regression 全方向カバー。
- **per-task gate (lint / typecheck / test / build)** 全 exit 0 + test 7/7 pass (1.62s)。

## Issues

### Critical (Must Fix)
なし。

### Important (Should Fix)
なし。

### Minor (Nice to Have)

1. **test 番号順序 (`exam-list-live.test.tsx:218 / :276`)**: file 内 describe block の番号順が `1, 2, 3, 4, 5, 7, 6` になっていた (`#7` を `#6` の前に挿入したため)。 番号は文中で参照される ID のため、 物理位置とズレると後続 task の grep / 突合 に支障。 **fix**: `#7` を `#6` の **後** に移動 (内容は無変更、 位置のみ swap)。 → **本 commit で吸収済** (swap で `5 → 6 → 7` 順に修正、 test 7/7 再走 pass 確認)。

2. **`inline-card-list.tsx:91` の `db.cards.where('exam_id').equals(examId).toArray()` への波及**: 本 task scope **外** (`/app/exams/[id]` であって `/app/exams` ではない、 plan T-B4 file リスト外)。 v6 schema 追加で同 index 経路を取れるため、 T-B5 / T-B6 / T-B7 で grep 可能性を session log 側に記録余地。 → **本 task では fix しない** (scope 外、 該当 task で別途検討)。

3. **spec §7-1 「Dexie 既存 cards 2k 件以上の reindex 負荷」 への smoke 計画記述**: code 側ではなく stg smoke runbook 側で確認する設計。 「v6 migration 後の初回 page load 時間」 を明示計測する 1 行を runbook に入れておくと spec §7-1 risk 確認が完結。 → **本 task では fix しない** (push 後の controller / OT 工程で stg smoke runbook 側に追加)。

## Recommendations

- Minor 1 を吸収済。
- spec §7-2 「Promise.all 同一 transaction 内逐次化」 risk は native count() なら μs オーダーで実害なしの判断。 stg 実機計測 (push 後工程) で「100 exam × N cards」 fixture の dexie_render_attr が想定 (a) との比較 (spec §2.5 / §7-3) を含む形になっているか session log で確認。
- T-B4 で追加した v6 index と T-B3 三段クローズ lesson (driver / DB 層変更) は同系統 (client mirror の schema-level 変更) なので、 三段クローズ lesson 側に「Dexie 純粋 index 追加は upgrade callback 不要、 ただし stg 実機で初回 reindex 時間を smoke する」 を 1 行追記余地あり。 → **別 task** (本 task では handle しない)。

## Assessment

**Ready to merge?** **Yes** (Critical 0 / Important 0、 Minor 1 を実装で吸収、 Minor 2-3 は scope 外)。

**Reasoning**: 最重要 self-check 2 件は Dexie 内部 source 突合で **コード構造レベル** で成立を独立検証済 = materialize 0 / owner isolation 両方 OK。 plan T-B4 + spec 2026-06-13 完了条件 ① ② (regression test 1 件 + 既存 6 件全 pass) は実走 verify 済。 per-task gate 全 exit 0。 完了条件 ③ stg 実機計測 / ④ (a)/(b) 比較は本 task subagent scope **外** (controller / OT 工程、 push 後実施)。

## 参照

- `lib/client-db.ts:282-294` (v6 schema)
- `app/(app)/app/exams/_components/exam-list-live.tsx:26-57` (新経路 + コメント)
- `app/(app)/app/exams/_components/exam-list-live.test.tsx` (test #6 owner isolation + #7 regression、 swap 後の物理順 `5 → 6 → 7`)
- `node_modules/dexie/dist/dexie.js:1981-1984` (`isPlainKeyRange`) + `:2108-2132` (`Collection.prototype.count`) — materialize 0 確証
- `docs/superpowers/plans/2026-06-12-y2-launch-hardening-B-perf.md:76-85` (T-B4 task block)
- `docs/superpowers/specs/2026-06-13-y2-t-b4-design.md` (本 task 方針確定 spec)
