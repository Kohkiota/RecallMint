# Codex independent review — ocr-2-4a-t8a-fix3 (2026-07-31)

- **作成日**: 2026-07-31
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The normalizer omits the established sort-key constraint, leaving a publish-time validation gap that defeats per-card isolation for oversized sort keys.

Review comment:

- [P1] Validate sort keys before emitting prepared cards — /workspaces/RecallMint/lib/ocr/normalize-prepared.ts:304-308
  When Gemini returns a `sort_key` longer than 100 characters, this hard-invariant block accepts it and later copies it into `PreparedCard.sortKey`, even though the publish boundary's `sortKeySchema` rejects it. That allows one malformed card to survive normalization and potentially abort atomic publication; apply `sortKeySchema` here alongside the other shared field schemas.