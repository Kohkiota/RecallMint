# Codex independent review — ocr-2-4b-t7-r2 (2026-08-08)

- **作成日**: 2026-08-08
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The new action accepts and persists PDF submissions, but the asynchronous pipeline explicitly ignores all PDF metadata and content. This can produce incomplete or failed processing after reporting the submission as accepted.

Review comment:

- [P1] Reject PDF submissions until the pipeline consumes them — /workspaces/RecallMint/app/(app)/app/upload/_lib/upload-pipeline.ts:174-181
  When `sourcePdfManifest` is non-empty, these arguments are discarded and `runOcrPhase` receives only the image files. A mixed submission can therefore complete while silently omitting every PDF, while a PDF-only submission runs OCR with an empty input despite the action returning `accepted`. Either implement PDF retrieval/rendering here or prevent the submit action from accepting PDFs until that processing exists.