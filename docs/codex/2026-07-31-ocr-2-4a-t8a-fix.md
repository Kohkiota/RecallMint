# Codex independent review — ocr-2-4a-t8a-fix (2026-07-31)

- **作成日**: 2026-07-31
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

A rejected card can permanently reserve its generated ID, causing a later valid card to be excluded incorrectly. Tests and type checking otherwise pass.

Review comment:

- [P2] Reserve card IDs only after the card passes UID validation — /workspaces/RecallMint/lib/ocr/normalize-prepared.ts:325-325
  When a card receives a valid `cardId` but is subsequently dropped because `nextId` produces malformed or duplicate option UIDs, this line leaves that ID in `seenCardIds`. If a later otherwise-valid card receives the same ID, it is incorrectly dropped as a duplicate even though the first card is absent from the result. Add the ID to the shared set only after all card-level validation succeeds, or remove it on the failure path.