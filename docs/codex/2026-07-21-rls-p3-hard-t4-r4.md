# Codex independent review — rls-p3-hard-t4-r4 (2026-07-21)

- **作成日**: 2026-07-21
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The restriction works for its configured scopes, but executable production scripts remain outside the lint rule. This leaves a direct bypass in the intended repo-wide enforcement.

Review comment:

- [P2] Cover executable scripts with the getDb ban — /workspaces/RecallMint/eslint.config.mjs:583-590
  The new scopes still omit `scripts/**/*.ts`, even though this directory contains executable operational modules such as `gc-image-assets.ts`, backfills, and `seed-perf-exam.ts`. Those files can therefore import `getDb` without an ESLint error, leaving a bypass in the claimed repo-wide production restriction; apply the ban to non-test scripts while preserving their test exemptions.