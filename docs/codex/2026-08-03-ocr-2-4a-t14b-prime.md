# Codex independent review — ocr-2-4a-t14b-prime (2026-08-03)

- **作成日**: 2026-08-03
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The primary purge paths are generally wired correctly, but superseded operations can commit as terminal without triggering immediate source cleanup whenever preparation subsequently exits with a non-success outcome.

Review comment:

- [P2] Purge superseded sources on early-return outcomes — /workspaces/RecallMint/app/(app)/app/upload/_actions/prepare-upload.ts:505-506
  When stale operations are superseded and the transaction later returns `in_progress`, `invalid_input`, `size_exceeded`, or `exam_not_found`, those terminal transitions still commit, but this success-only branch never receives or purges their source documents. This defeats the new post-terminal purge path and leaves the source objects in R2 until the periodic reconciler runs; preserve the superseded IDs across every post-supersede outcome or perform the purge independently of the final preparation outcome.