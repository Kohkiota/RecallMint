# Codex independent review — row-ux-task4 (2026-08-15)

- **作成日**: 2026-08-15
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The main interaction changes pass the targeted tests and static checks, but the shared card-label helper does not implement its documented blank-label fallback, causing incorrect labels in the newly added DnD announcements.

Review comment:

- [P2] Fall back to the title for blank question labels — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-row-menu.tsx:452-453
  When `question_label` is an empty or whitespace-only string but `title` is populated, the nullish-coalescing expression selects `question_label`, then produces `'(無題)'` instead of the title. This now also makes the new DnD announcements read the wrong card name; explicitly choose the first non-blank value after trimming.