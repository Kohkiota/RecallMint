# Codex independent review — ocr-2-4a-t14a-fix3 (2026-08-02)

- **作成日**: 2026-08-02
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The new deadline mechanism only bounds a single publish invocation, while the stated operation-wide deadline resets on every retry or takeover. Other reviewed changes and tests appear consistent.

Review comment:

- [P2] Preserve one deadline across publish retries — /workspaces/RecallMint/app/(app)/app/upload/_actions/publish-prepared.ts:311-314
  Each invocation computes a fresh deadline from `Date.now()`, so a retry or lease takeover resets the full crop budget. For operations that repeatedly return `figure_retryable` or are taken over, crop work can therefore consume another 10 minutes on every attempt instead of respecting the documented operation-wide absolute deadline, potentially continuing until the seven-day retention cap. Persist or derive a stable deadline from operation data rather than recreating it per action call.