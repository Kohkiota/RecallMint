# Codex independent review — ocr-2-4a-t14b (2026-08-02)

- **作成日**: 2026-08-02
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The source-lane dry-run does not preview all destructive work that the corresponding real sweep performs, specifically retries of already-deleting sources.

Review comment:

- [P2] Include already-deleting sources in the dry-run preview — /workspaces/RecallMint/scripts/gc-image-assets.ts:585-596
  When a previous sweep leaves a source in `deleting` (for example after an R2 failure or crash), the next real sweep collects and deletes it, but this dry-run returns after examining only `reserved|ready` promotion candidates. As a result, `--sweep --dry-run` can report zero reclaimed objects immediately before a real run deletes existing `deleting` rows and their R2 objects, defeating the preview's purpose; fetch and report collect candidates without performing writes.