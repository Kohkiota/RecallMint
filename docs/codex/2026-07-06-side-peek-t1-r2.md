# Codex independent review — side-peek-t1-r2 (2026-07-06)

- **作成日**: 2026-07-06
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The side peek still has a functional lost-edit path when switching between cards while an option cell is being edited. That should be fixed before the patch is considered correct.

Review comment:

- [P2] Preserve option edits when switching cards — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-side-peek.tsx:102-102
  When the parent changes `row` while the peek remains open (for example, selecting another table row), this keyed wrapper remounts the option list without going through `onOpenChange`, so the explicit `activeElement.blur()` close safeguard is skipped. If an option id/text/explanation cell is focused, `InlineOptionCell` only saves on blur and has no unmount commit path, so the in-progress edit can be discarded during card switching.