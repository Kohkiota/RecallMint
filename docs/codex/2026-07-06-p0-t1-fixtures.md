# Codex independent review — p0-t1-fixtures (2026-07-06)

- **作成日**: 2026-07-06
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The added review-events fixture does not faithfully model the route's bulk update return values, which can make future contract tests fail or assert the wrong behavior when extra cards are seeded.

Review comment:

- [P2] Return only IDs included in the VALUES update — /workspaces/RecallMint/tests/fixtures/review-events.ts:256-258
  When a review-events contract test seeds `state.cardRows` with extra cards that are not part of the current payload, this mock returns every seeded card ID from `.returning()` rather than only the IDs present in the captured `VALUES` SQL. The route compares the returned ID count with `finalStates.size`, so valid single-card updates can be reported as `bulk update card count mismatch` whenever unrelated card rows are also in fixture state.