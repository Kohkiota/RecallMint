# Codex independent review — side-peek-t2 (2026-07-06)

- **作成日**: 2026-07-06
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The change introduces a user-visible button in the production table before its required callback is provided, making the new control nonfunctional.

Review comment:

- [P2] Render the peek button only when a handler is wired — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-table-columns.tsx:123-128
  In the real table `meta` is always supplied by `exam-card-table.tsx`, but this patch does not add `openCard` there, so every title cell now renders a visible `カードを開く` button whose click handler is a no-op. Until the side peek is actually wired, this should be gated on `meta.openCard` rather than just `meta` to avoid exposing a nonfunctional control.