# Codex independent review — edit-3-t4 (2026-06-30)

- **作成日**: 2026-06-30
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The default-hidden change works for fresh state, but it breaks restoration of existing saved preferences where no columns are hidden. That means persisted user column visibility can be ignored after this patch.

Review comment:

- [P2] Respect saved empty visibility preferences — /workspaces/RecallMint/app/(app)/app/exams/[id]/_components/exam-card-table.tsx:81-81
  With `columnVisibility` now initialized to `{ sort_key: false }`, a saved `examViewPrefs` record whose `hiddenColumns` is `[]` no longer restores “all columns visible”: the load effect only calls `setColumnVisibility` when `hiddenColumns.length > 0`, so users who previously saved no hidden columns will still get `sort_key` hidden. Treat an existing saved record as authoritative even when the hidden list is empty, otherwise persisted visibility preferences are ignored for this common case.