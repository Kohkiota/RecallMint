# Codex independent review — r0-runbook-14-4 (2026-08-16)

- **作成日**: 2026-08-16
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The SQL change is comment-only, but the new operational readback claims to validate `WITH CHECK` equality while omitting that expression. This can produce a false-positive policy verification.

Review comment:

- [P2] Include WITH CHECK in the parent-policy comparison — /workspaces/RecallMint/docs/ops/rls-p2-stg-runbook.md:649-651
  This query is presented as verifying that `review_logs` and `answer_events` match across five policy elements, including `WITH CHECK`, but it only selects `polqual` (`USING`). If either policy's `polwithcheck` drifts, this readback still appears successful; select `pg_get_expr(p.polwithcheck, p.polrelid)` as well so the documented comparison actually covers all five elements.