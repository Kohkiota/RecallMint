# Codex independent review — ocr-2-4a-s1 (2026-08-04)

- **作成日**: 2026-08-04
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The route timeout now coincides with the OCR pipeline deadline, removing the buffer required for error handling and persistence. Near-timeout uploads can therefore be killed before cleanup or results are committed.

Review comment:

- [P1] Preserve headroom beyond the OCR deadline — /workspaces/RecallMint/app/(app)/app/upload/page.tsx:21-21
  With the route timeout reduced to 720 seconds while `OCR_OVERALL_DEADLINE_MS` remains exactly 720 seconds, uploads that approach the OCR deadline can be terminated by the platform before the action catches the timeout and performs card insertion or failure-state cleanup. This also affects the currently connected legacy upload path, not just the new stub, and can leave operations/documents stuck in processing; keep `maxDuration` above the internal pipeline deadline or shorten that deadline to retain post-processing headroom.