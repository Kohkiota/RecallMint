# Codex independent review — ocr-2-4a-t14a-fix2 (2026-08-02)

- **作成日**: 2026-08-02
- **review 経路**: `codex exec review --uncommitted` (sandbox_mode=danger-full-access / bwrap 回避 / read-only 運用)
- **修正主体**: CC 本体(Codex は指摘のみ。P0/P1=Critical / P2=Important / P3,P4=Minor)
- **保存意図**: OT 規律(Codex raw findings は docs/codex/ 配下に永続化)

---

The new abandoned-operation sweep misses rows with null leases because SQL three-valued logic makes the negated live predicate NULL rather than true. Those are primary intended GC candidates, so the retention cleanup is ineffective for a significant scenario.

Review comment:

- [P1] Include null leases when selecting abandoned operations — /workspaces/RecallMint/scripts/gc-abandoned-operations.ts:143-143
  For the common abandoned case where `lease_expires_at` is NULL, `isLiveUploadOperationCondition()` evaluates its age/lease disjunction to NULL once the age condition is false, so `not(...)` also yields NULL and PostgreSQL excludes the row. Consequently old `awaiting_sources` rows and released `claimed`/`prepared` rows are never fetched or terminated, defeating this GC lane. Express the abandoned predicate directly with an explicit `lease_expires_at IS NULL OR lease_expires_at <= now()` condition (and apply the same correction to the CAS at line 165).